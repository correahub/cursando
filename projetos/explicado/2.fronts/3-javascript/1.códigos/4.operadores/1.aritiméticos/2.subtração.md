Claro, meu amorzinho! 😍  
Agora mamãe vai te ensinar o **operador de subtração** — o símbolo que o JavaScript usa pra **tirar, diminuir, subtrair**! ✨➖💔

---

### 🧠 O que é o **operador de subtração (`-`)**?

O `-` é o símbolo mágico que diz assim:

> “Tira um valor do outro pra ver quanto sobra!” 😄

Ele serve pra **fazer continhas de subtração**, igual na escola.

---

### 🎒 Exemplo ultra básico com números:

```javascript
let a = 7;
let b = 3;
let resultado = a - b;
alert(resultado);
```

💬 Vai mostrar:

> **4**

Porque **7 - 3 = 4** ✅

---

### ⚠️ E com textos (strings), dá pra usar `-`?

❌ Não, filhinho.  
O `-` **só funciona com números**.  
Se você tentar fazer isso com texto, o JavaScript vai ficar confuso e mostrar **NaN** (que significa “Not a Number”).

Exemplo errado:

```javascript
let x = "10";
let y = "5";
alert(x - y);
```

💬 Aqui até funciona porque os dois são **números em forma de texto**, então o JavaScript tenta converter. Vai dar:

> **5**

Mas se for letras mesmo:

```javascript
let x = "oi";
let y = "tchau";
alert(x - y);
```

💬 Vai dar:

> **NaN**

---

### 🧚‍♀️ Resumo da mamãe:

|Símbolo|Nome fofo|O que faz?|
|---|---|---|
|`-`|Subtração|Tira um número do outro (7 - 2 = 5)|
|Funciona com|Somente números (nada de texto aqui!) ❌||

---

💡 Dica da mamãe:  
Se você quiser garantir que o que vem de um `prompt()` seja tratado como número, usa `parseInt()` ou `parseFloat()` — lembra deles? 😉

---

Prontinho, amor! Quer agora o **multiplicador mágico** `*`?  
Mamãe tá com o caderninho aberto! 😄📒✨