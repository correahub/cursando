Aaaah, que alegria, meu filhinho! 😍  
Agora mamãe vai te ensinar o **operador de resto da divisão**, que é o **`%`** — também conhecido como o **operador módulo**! ✨🔢🥣

---

### 🧠 O que é o operador **`%`**?

Esse símbolo mágico **não divide de verdade**.

Ele faz outra coisa:

> Ele mostra **o que sobrou** depois de uma divisão! 😄

---

### 🎒 Exemplo simples:

```javascript
let resultado = 10 % 3;
alert(resultado);
```

💬 Vai mostrar:

> **1**

Porque:

- **10 dividido por 3** dá **3**, e sobra **1**
    
- Então, o **resto da divisão é 1**
    

---

### 🧁 Outro exemplo:

```javascript
let resultado = 8 % 2;
alert(resultado);
```

💬 Vai mostrar:

> **0**

Porque **8 dividido por 2 dá 4 certinho**, e **não sobra nada**!

---

### 💡 Quando usar?

O `%` é super útil pra:

|Situação|Exemplo|
|---|---|
|Ver se um número é **par ou ímpar**|`número % 2 === 0` (par)|
|Fazer jogos que rodam em **ciclos**|tipo “a cada 3 rodadas”|
|Controlar colunas, páginas, voltas...|Contador de 0 até 9 e volta|

---

### 🧸 Exemplo: número par ou ímpar

```javascript
let numero = parseInt(prompt("Digite um número"));
if (numero % 2 === 0) {
  alert("Esse número é par!");
} else {
  alert("Esse número é ímpar!");
}
```

💬 Vai dizer se o número é par ou ímpar usando o `%` 😄

---

### 🧚‍♀️ Resumo da mamãe:

|Símbolo|Nome mágico|O que faz?|
|---|---|---|
|`%`|Módulo ou Resto|Mostra o que sobra depois de uma divisão|
|Ex:|`10 % 3 = 1`|Porque 10 ÷ 3 = 3, sobra 1|
|Muito usado pra|Saber se número é par/ímpar ou controlar ciclos 🔄||

---

Você arrasou até aqui, meu campeão do JavaScript! 👑✨  
Quer agora o resumo de **todos os operadores aritméticos** juntos pra você guardar no caderno da mamãe? 😄📘💗