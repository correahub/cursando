Aaaah, agora sim! 😍  
Mamãe vai te ensinar o irmãozinho do `+=`… o **`-=`**!  
Ele é o operador que **tira** alguma coisa da variável. É o sinalzinho de **subtração acumulada**! ✨➖🐣

---

### 🧠 O que é o `-=`?

O `-=` é um **atalho para subtrair** um valor de uma variável.  
É como dizer:

> “Tira isso do que já está guardado aqui!”

---

### 🎒 Exemplo ultra básico:

```javascript
let balas = 10;
balas -= 3;  // é o mesmo que: balas = balas - 3
alert(balas);
```

💬 Vai mostrar:

> **7**

Porque:

- O bebê tinha **10 balas** 🍬
    
- A mamãe pegou **3** 😅
    
- Sobraram **7 balas!** 🍬🍬🍬🍬🍬🍬🍬
    

---

### 🧁 Outro exemplo com número vindo do usuário:

```javascript
let dinheiro = parseFloat(prompt("Quanto você tem?"));
dinheiro -= 5;  // Gastou 5 reais
alert(`Agora você tem R$ ${dinheiro}`);
```

Se você digitar `20`, o alerta será:

> **"Agora você tem R$ 15"**

---

### ✨ Forma completa (sem `-=`):

```javascript
balas = balas - 3;
```

Mas com o `-=` fica bem mais curto e fofo:

```javascript
balas -= 3;
```

💨 Economiza tempo e deixa o código mais limpo!

---

### 🧚‍♀️ Resumo da mamãe:

|Símbolo|Nome fofo|O que faz?|
|---|---|---|
|`-=`|Subtração acumulada|Tira um valor do que já estava guardado|
|Mesmo que|`x = x - y`||
|Funciona com|Números (não funciona com texto, tá?) ❌||

---

Agora mamãe pode te mostrar o `*=`, que **multiplica e guarda**, e o `/=`, que **divide e guarda**.  
Você quer aprender os dois, meu docinho de leite? 😄✨🥛