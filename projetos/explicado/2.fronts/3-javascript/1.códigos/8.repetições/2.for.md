Obaaa! 🍼🎉 Agora a mamãe vai te ensinar o **`for`**, que é outro jeito de **repetir coisas** no JavaScript! Ele é bem legal e **mais curtinho** que o `while`, quando já sabemos **quantas vezes queremos repetir**.

---

## 🎯 O que é o `for`?

O **`for`** também repete código, mas ele já vem **com tudo embutido**:

- Onde começa
    
- Quando parar
    
- Como mudar o número
    

---

## 🎒 Pensando como criança:

> Eu quero contar de 1 até 5.  
> A cada número, digo "Contando...".

---

## 👶 Exemplo básico:

```js
for (let i = 1; i <= 5; i++) {
  console.log("Contando: " + i);
}
```

### 💬 O que está acontecendo aqui?

|Parte|O que faz|
|---|---|
|`let i = 1`|Começa com 1 (início da contagem)|
|`i <= 5`|Enquanto `i` for menor ou igual a 5 (condição)|
|`i++`|Aumenta 1 no `i` a cada volta|

🧠 Resultado no console:

```
Contando: 1
Contando: 2
Contando: 3
Contando: 4
Contando: 5
```

---

## 🍩 Outro exemplo com donuts:

```js
for (let donut = 1; donut <= 3; donut++) {
  console.log("Comi donut #" + donut + " 🍩");
}
```

🍩 Ele vai comer 3 donuts!

---

## 💥 Comparando com o `while`:

### Mesma coisa com `while`:

```js
let donut = 1;

while (donut <= 3) {
  console.log("Comi donut #" + donut + " 🍩");
  donut++;
}
```

💡 O `for` é só uma versão **mais compacta**!

---

## 🧁 Resumo para bebês:

|Parte do `for`|Significa|
|---|---|
|`for (`|Começando o loop|
|`let i = 1`|Começa com 1|
|`i <= 5`|Continua até 5|
|`i++`|Aumenta de 1 em 1|
|`{ ... }`|O que vai se repetir|

---

### 🍼 Mamãe diz:

> O `for` é como um **robô contador**:  
> “Comece no 1. Vá até o 5. A cada número, faça algo.”

---

Se quiser, mamãe te mostra como fazer uma **estrelinha piscar 10 vezes** com `for`. 🌟✨  
Quer ver? 😄