Claro, meu bebê programador! 👶🍼 Agora a mamãe vai te ensinar o **`while`**, que é uma **forma de repetir coisas** no JavaScript! 🎉

---

## 🌀 O que é o `while`?

A palavra **`while`** significa **“enquanto”**.

Então o `while` diz assim:

> **Enquanto** uma coisa for verdadeira, **repita esse código**.

---

### 🧸 Pensando como criança:

> **Enquanto** eu tiver balas no pote, eu como uma bala 🍬

---

## 🍬 Exemplo simples:

```js
let balas = 3;

while (balas > 0) {
  console.log("Comer uma bala 🍬");
  balas = balas - 1;
}
```

### 🧠 O que acontece aqui?

1. O número de balas começa em **3**.
    
2. O `while` pergunta: **"balas > 0?"**
    
3. Sim? Então **come uma bala** e **tira 1** da contagem.
    
4. Repete até **não ter mais balas**.
    

Resultado:

```
Comer uma bala 🍬
Comer uma bala 🍬
Comer uma bala 🍬
```

---

## ⚠️ Muito importante!

Se você esquecer de **diminuir o número**, o `while` **nunca para** e vira um **loop infinito! 😱**

Tipo assim (⚠️ errado!):

```js
let balas = 3;

while (balas > 0) {
  console.log("Comendo balas para sempre! 🍬");
  // balas nunca diminui, então repete para sempre!
}
```

---

## 🎮 Exemplo divertido: energia do boneco

```js
let energia = 5;

while (energia > 0) {
  console.log("Pulando! 🦘 Energia restante: " + energia);
  energia--; // tira 1 de energia a cada pulo
}
```

👆 O bonequinho pula até a energia acabar.

---

## 🧁 Resumo de `while` para bebês:

|Parte|O que faz|
|---|---|
|`while (condição)`|Enquanto essa condição for verdadeira...|
|`{ código }`|...repita esse código várias vezes|
|⚠️|Se nunca mudar a condição, vai repetir pra sempre!|

---

### 🍼 Mamãe explica assim:

> "Enquanto algo for verdade, repete. Quando ficar falso, para."

---

Se quiser, posso mostrar um joguinho de contar estrelas ⭐ com `while`. Quer ver também? ✨