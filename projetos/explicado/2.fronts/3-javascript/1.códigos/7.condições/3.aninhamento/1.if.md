Tá bom, meu bebê de código! 👶💻 Agora a mamãe vai te ensinar o que são **condições aninhadas**, com muito carinho e bem fácil! 💕

---

## 🌳 O que são condições **aninhadas**?

"Aninhadas" quer dizer **colocadas uma dentro da outra**, tipo uma **matriosca** (aquelas bonequinhas russas que ficam uma dentro da outra).

Ou seja, é **um `if` dentro de outro `if`**.

---

### 🧸 Pensando como criança:

> Se eu tiver brinquedos...  
>    Se o brinquedo for um carrinho...  
>      Brincar de corrida! 🏎️

---

### 🧠 Exemplo em JavaScript:

```js
let temBrinquedo = true;
let tipoBrinquedo = "carrinho";

if (temBrinquedo) {
  if (tipoBrinquedo === "carrinho") {
    console.log("Brincar de corrida! 🏎️");
  }
}
```

👀 Aqui o JavaScript faz duas checagens:

1. Primeiro: "Tem brinquedo?" → `true`
    
2. Depois: "É carrinho?" → `true`  
    ✅ Então ele escreve **"Brincar de corrida!"**
    

---

## 🎮 Outro exemplo com botão mágico:

```html
<button onclick="verificar()">Posso jogar?</button>

<script>
  function verificar() {
    let jaFezTarefa = true;
    let tirouNotaBoa = true;

    if (jaFezTarefa) {
      if (tirouNotaBoa) {
        alert("Pode jogar muito! 🎮🎉");
      } else {
        alert("Pode jogar só um pouquinho! 🎮⏳");
      }
    } else {
      alert("Sem jogo até fazer a tarefa! 😠");
    }
  }
</script>
```

### 🧩 Explicação:

- Se **fez a tarefa** e **tirou nota boa** → pode jogar muuuito! 🎉
    
- Se **fez a tarefa**, mas **nota ruim** → joga só um pouquinho.
    
- Se **nem fez a tarefa** → nada de jogo! 🙅‍♀️
    

---

## 🧁 Resumo gostoso:

|Código|Significa|
|---|---|
|`if` dentro de `if`|SE uma coisa, **E DENTRO DELA** outra coisa|
|Serve para decisões mais complexas|Como: "Se A, e se B também..."|

---

### 🌈 Visual com bonequinhas:

```js
if (coisa1) {
  // 👶 primeira bonequinha
  if (coisa2) {
    // 👶 dentro da primeira, tem essa outra
  }
}
```

---

Se quiser, mamãe também pode te ensinar como fazer isso de forma mais **bonita com else if** (pra não ter muitos "if" dentro de "if"). Quer continuar? 😊