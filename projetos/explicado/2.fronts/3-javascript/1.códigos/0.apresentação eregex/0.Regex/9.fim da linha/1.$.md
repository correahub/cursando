Show, filho! Agora vamos aprender sobre o símbolo **`$`** no Regex, que é irmão do **`^`** que a gente viu antes. ⚡️

---

## 🧠 O que significa o `$` no Regex?

O `$` significa:

> 👉 **“Termina com”**

Ou seja, você quer que o texto ou a linha termine exatamente com aquilo que vem antes do `$`.

---

## 🎯 Vamos entender com exemplos?

---

### Exemplo 1: `a$`

Quer dizer:

> Quero encontrar textos ou linhas que **terminem com a letra `a`**.

Texto:

```
banana, maçã, abacaxi, uva, melão
```

Regex `a$` vai encontrar:

✅ `banana` (termina com `a`)  
✅ `maçã` (termina com `a`)  
❌ `abacaxi` (termina com `i`)  
✅ `uva` (termina com `a`)  
❌ `melão` (termina com `o`)

---

### Exemplo 2: `[0-9]$`

Quer dizer:

> Quero linhas que **terminem com um número** (de 0 a 9).

Texto:

```
item 7, teste 3, ano 2021, código A1
```

Regex `[0-9]$` vai encontrar:

✅ `item 7` (termina com `7`)  
✅ `teste 3` (termina com `3`)  
✅ `ano 2021` (termina com `1`)  
❌ `código A1` (termina com `1`? Na verdade termina com `1`, mas será que? Vamos pensar...)

Ah! Aqui o texto termina com `1` mesmo, então ✅ também.

---

### Exemplo 3: `abc$`

Quer dizer:

> Quero textos que terminem com `abc`.

Texto:

```
xyzabc, abc, abc123, xyz
```

Regex `abc$` vai encontrar:

✅ `xyzabc` (termina com `abc`)  
✅ `abc` (termina com `abc`)  
❌ `abc123` (termina com `123`)  
❌ `xyz` (não termina com `abc`)

---

## 👶 Resumo de criança

|Regex|Significado|Exemplo|
|---|---|---|
|`a$`|Termina com a letra “a”|banana, maçã, uva|
|`[0-9]$`|Termina com um número|item 7, ano 2021|
|`abc$`|Termina com “abc”|xyzabc, abc|

---

Quer que eu te dê exercícios para praticar o `$`? Ou quer aprender outro símbolo mágico do Regex?  
Tô aqui pra ajudar! 😄