Show, filho! Agora vamos aprender sobre o **^** no Regex, que é um símbolo muito importante! ⚡️

---

## 🧠 O que significa o `^` no Regex?

O `^` pode significar duas coisas, dependendo de onde ele estiver:

### 1. Quando está **no começo** do regex

Ele significa:

> 👉 **“Começa com”**

Ou seja, você quer que o texto ou a linha comece exatamente com aquilo que vem depois do `^`.

---

### 2. Quando está **dentro de colchetes `[]` no começo**

Ele significa:

> 👉 **“Não pode ter esses caracteres”**

Por exemplo, `[^abc]` quer dizer **qualquer caractere que não seja `a`, `b` ou `c`**.

---

## 🎯 Vamos entender com exemplos?

---

### Exemplo 1: `^a`

Quer dizer:

> Quero encontrar textos ou linhas que **comecem com a letra `a`**.

Texto:

```
ana, banana, abacaxi, maça
```

Regex `^a` vai encontrar:

✅ `ana` (começa com `a`)  
❌ `banana` (começa com `b`)  
✅ `abacaxi` (começa com `a`)  
❌ `maça` (começa com `m`)

---

### Exemplo 2: `^[0-9]`

Quer dizer:

> Quero linhas que **comecem com um número** (de 0 a 9).

Texto:

```
7 dias, hoje é 3, 123, abc
```

Regex `^[0-9]` vai encontrar:

✅ `7 dias` (começa com número)  
❌ `hoje é 3` (começa com letra)  
✅ `123` (começa com número)  
❌ `abc` (começa com letra)

---

### Exemplo 3: `[^aeiou]`

Aqui o `^` está dentro do colchete no começo. Significa:

> Quero qualquer caractere que **não seja** vogal (a, e, i, o, u).

Texto:

```
a, b, c, e, f, o, u
```

Regex `[^aeiou]` vai encontrar:

✅ `b`, `c`, `f` (não são vogais)  
❌ `a`, `e`, `o`, `u` (vogais são ignoradas)

---

## 👶 Resumo de criança

|Posição do `^`|Significado|Exemplo|
|---|---|---|
|No começo|Linha/texto começa com isso|`^a` acha palavras que começam com “a”|
|Dentro de `[]` no começo|Negação (não pode ter esses caracteres)|`[^aeiou]` acha letras que não são vogais|

---

Quer que eu te dê exercícios para praticar o `^`? Ou quer aprender outro símbolo mágico do Regex?  
Tô aqui! 😄