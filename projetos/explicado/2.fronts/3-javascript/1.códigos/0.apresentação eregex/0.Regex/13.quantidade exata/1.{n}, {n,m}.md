### Lembra do nosso **caçador de palavras**?

Os colchetes `{}` viram a **regra de “quantas vezes”** ele deve pegar algo.  
Pense neles como um bilhete escrito assim:

|Bilhete|Significado|
|---|---|
|`{3}`|**Exatamente 3 vezes**|
|`{3,}`|**Pelo menos 3 vezes** (3, 4, 5…)|
|`{2,5}`|**Entre 2 e 5 vezes** (2, 3, 4 ou 5)|

---

## 1. `{n}` – “pegue **n** porções e só”

- **Padrão:** `a{3}`
    
- **Fala em voz alta:** “a três vezes”
    
- **Funciona em:** `aaa`
    
- **Não funciona em:** `aa` (duas), `aaaa` (quatro)
    

💡 _Dica visual:_ imagine empilhar bloquinhos. O caçador só entrega se vir **exatos três**.

---

## 2. `{n,}` – “no mínimo **n** porções”

- **Padrão:** `bo{2,}`
    
- **Lê:** “o duas ou mais vezes”
    
- **Bate em:** `boo`, `booo`, `boooooooo…`
    

É como dizer: “traga uma caixa cheia; se vier lotada, melhor ainda!”

---

## 3. `{n,m}` – “qualquer coisa entre **n** e **m**”

- **Padrão:** `ha{1,4}`
    
- **Lê:** “a entre uma e quatro vezes”
    
- **Bate em:**
    
    - `ha` (1 a)
        
    - `haa` (2 a’s)
        
    - `haaa` (3)
        
    - `haaaa` (4)
        
- **Não bate em:** `haaaaa` (5 – passou do limite)
    

Como um parque que só deixa entrar grupos de 1 a 4 crianças: 0 ou 5? Portão fechado.

---

## 4. Misturando com outros símbolos

- **`[0-9]{2,4}`** → “2 a 4 dígitos” (`99`, `123`, `2025`)
    
- **`-?\\d{3}`** → “sinal de menos opcional + exatamente 3 dígitos” (`123`, `-456`)
    
- **`<.*?>`** → Aqui o `{}` não aparece, mas note o `?` depois de `*`: lembra que ele deixa o caçador preguiçoso. Se você fizer **`a{2,5}?`** vale igual — o caçador pega de 2 a 5, mas para na menor quantidade possível que dê certo.
    

---

## 5. Laboratório de bolso

1. **Texto-alvo**
    
    ```
    banana
    bananaa
    banaaana
    ```
    
2. **Padrão** `na{2}`
    
3. **Resultado mental**
    
    - `banana` → acha `na`? precisa de 2 a’s seguidas: **não**
        
    - `bananaa` → acha `naa` (n + 2 a’s): **sim**
        
    - `banaaana` → primeira batida em `naa`, depois outra em `naaa`? depende do motor, mas ambas respeitam “duas a’s”.
        

Brinque em sites como **Regex101**: cole o texto, digite o padrão e veja as partes coloridas.

---

## 6. Resumindo bem curtinho

> **`{}` = quantas vezes?**
> 
> - `{n}` → exatamente n.
>     
> - `{n,}` → pelo menos n.
>     
> - `{n,m}` → de n até m.
>     

Com isso você controla o caçador: “só aceite se vir três letras!”, ou “traga qualquer coisa maior que cinco números!”, ou “admito de dois a quatro risadas hahaha”. Agora é só praticar! 🎯