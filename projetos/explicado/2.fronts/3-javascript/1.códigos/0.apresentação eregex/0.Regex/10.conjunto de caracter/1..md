Show, filho! Agora vamos aprender sobre os **colchetes `[ ]`** no Regex, que são super úteis para escolher várias opções de uma vez só! 🎯✨

---

## 🧠 O que significam os colchetes `[ ]` no Regex?

Os colchetes `[ ]` servem para dizer:

> 👉 “Quero encontrar **qualquer um dos caracteres que estiver dentro dos colchetes**.”

Ou seja, dentro do `[ ]` você coloca um grupo de caracteres, e o Regex vai combinar com **qualquer um deles**.

---

## 🎯 Exemplos fáceis para entender

### Exemplo 1: `[abc]`

Quer dizer:

> “Qualquer letra `a` ou `b` ou `c`.”

Texto:

```
gato, bola, casa, dedo
```

Regex `[abc]` vai encontrar:

✅ `a` (em gato)  
✅ `b` (em bola)  
✅ `a`, `c` (em casa)  
❌ `d`, `e`, `o` (em dedo, mas o `d` e `e` não estão no grupo)

---

### Exemplo 2: `[0-9]`

Quer dizer:

> “Qualquer número de 0 a 9.”

Texto:

```
ano 2023, idade 18, telefone 9999
```

Regex `[0-9]` vai encontrar:

✅ `2`, `0`, `2`, `3`, `1`, `8`, `9`, `9`, `9`, `9`

---

### Exemplo 3: `[a-z]`

Quer dizer:

> “Qualquer letra minúscula de a até z.”

Texto:

```
Olá Mundo!
```

Regex `[a-z]` vai encontrar:

✅ `l`, `a`, `u`, `n`, `d`, `o` (letras minúsculas)  
❌ `O` (maiúsculo não está no intervalo `a-z`), nem espaços ou símbolos

---

### Exemplo 4: Combinação: `[A-Za-z0-9]`

Quer dizer:

> “Qualquer letra maiúscula ou minúscula, ou número.”

Texto:

```
Oi123, TudoBem?
```

Regex `[A-Za-z0-9]` vai encontrar:

✅ Todas as letras `O`, `i`, `T`, `u`, `d`, `o`, `B`, `e`, `m`  
✅ Todos os números `1`, `2`, `3`  
❌ Espaço e `?` não são encontrados

---

## ⚠️ Dica importante: Negação dentro dos colchetes

Se você colocar um **^** logo depois do `[`, tipo `[ ^abc ]`, isso quer dizer:

> “Qualquer caractere **que não seja** `a`, `b` ou `c`.”

---

## 👶 Resumo de criança

|Regex|Significado|Exemplo que pega|
|---|---|---|
|`[abc]`|Qualquer `a`, `b` ou `c`|`a`, `b`, `c`|
|`[0-9]`|Qualquer número de 0 até 9|`0`, `1`, `2`, ..., `9`|
|`[a-z]`|Qualquer letra minúscula de a até z|`a`, `b`, `c`, ..., `z`|
|`[A-Za-z0-9]`|Qualquer letra maiúscula/minúscula ou número|`A`, `b`, `Z`, `3`|
|`[^abc]`|Qualquer coisa que **não** seja `a`, `b` ou `c`|`d`, `e`, `f`, `1`, `@`|

---

Quer que eu te dê exercícios para praticar os colchetes `[ ]`? Ou quer aprender outro símbolo mágico do Regex?  
Tô aqui pra ajudar! 😄