Boa, filho! 😄 Agora vamos conhecer o **irmão maluco do `\w`**, que é o **`\W`** (com **W maiúsculo**). Ele faz **o contrário** do que o `\w` faz!

---

## 🧠 O que significa `\W`?

`\W` no Regex quer dizer:

> 👉 "**Qualquer coisa que **não** seja letra, número ou underline**"

Ou seja, ele encontra:

- ❌ Nada de letras (A-Z, a-z)
    
- ❌ Nada de números (0-9)
    
- ❌ Nada de `_`
    
- ✅ Espaços
    
- ✅ Pontuações
    
- ✅ Símbolos como `!`, `@`, `#`, `$`, `-`, `,`, `.` etc.
    

---

## 📦 Exemplo simples

### Texto:

```
a_1! b2@ c-3#
```

### Regex: `\W`

Ele encontra:

✅ `!`  
✅ `@`  
✅ `-`  
✅ `#`  
✅ espaço entre `a_1` e `b2`, e entre `b2` e `c-3`

Ele **ignora**:

❌ `a`, `1`, `_`, `b`, `2`, `c`, `3` — porque tudo isso são letras, números ou underline (ou seja, coisas que o `\w` aceitaria).

---

## 🎯 Comparando `\w` e `\W`

|Caractere|`\w`|`\W`|
|---|---|---|
|`a`|✅|❌|
|`9`|✅|❌|
|`_`|✅|❌|
|`!`|❌|✅|
|`@`|❌|✅|
|espaço|❌|✅|
|`#`|❌|✅|

---

## 🧪 Exemplo com `\W+`

Se usarmos `\W+`, isso significa:

> “Me mostre **um ou mais símbolos, espaços ou coisas que não são letras/números/underline**.”

### Texto:

```
senha@123!
```

Regex: `\W+` encontra:

✅ `@`  
✅ `!`

---

## 👶 Resumo de criança:

|Regex|Significa|Exemplo que funciona|
|---|---|---|
|`\w`|Letra, número ou underline|`a`, `Z`, `1`, `_`|
|`\W`|Qualquer coisa que **não** seja isso|`!`, `@`, espaço, `#`|

---

Se quiser, posso te dar desafios com `\w` e `\W`, ou te ensinar o `\s`, que é o símbolo mágico pra encontrar **espaços em branco**! Quer aprender ele agora? 😊