Beleza, filho! Agora vamos aprender sobre os **parênteses `()`** no Regex, que são como uma caixinha mágica que guarda um pedaço especial do que você quer encontrar. 🎩✨

---

## 🧠 O que significam os parênteses `()` no Regex?

Os parênteses servem para:

> 👉 **Agrupar** partes do regex para tratar como uma única coisa.

Isso ajuda a:

- Fazer repetição de um grupo inteiro
    
- Guardar uma parte do texto encontrado para usar depois
    
- Organizar seu regex para ficar mais claro
    

---

## 🎯 Exemplos simples para entender

### Exemplo 1: Agrupar para repetir

Regex:

```
(ab)+
```

Quer dizer:

> O grupo `ab` pode aparecer **uma ou mais vezes seguidas**.

Texto:

```
ab, abab, ababab, a, b, aba
```

Vai encontrar:

✅ `ab`  
✅ `abab`  
✅ `ababab`  
❌ `a` (não tem o `b` junto)  
❌ `b` (sozinho não serve)  
❌ `aba` (porque termina com `a`, sem o `b`)

---

### Exemplo 2: Capturar partes para usar depois (isso é avançado, só pra saber)

Se você usar parênteses, o Regex “guarda” o que encontrou ali. Depois você pode usar para substituir ou usar em outras coisas, mas isso você aprende quando avançar mais.

---

### Exemplo 3: Agrupar para aplicar quantificadores

Regex:

```
(a|b)+
```

Quer dizer:

> Um ou mais caracteres que sejam **`a` ou `b`**.

Texto:

```
a, b, ab, ba, aaabbb, c, d
```

Vai encontrar:

✅ `a`  
✅ `b`  
✅ `ab`  
✅ `ba`  
✅ `aaabbb`  
❌ `c`  
❌ `d`

---

## 👶 Resumo de criança

| Símbolo | Função                    | Exemplo                               |
| ------- | ------------------------- | ------------------------------------- |
| `()`    | Agrupa um pedaço do regex | `(ab)+` repete “ab” uma ou mais vezes |
|         |                           |                                       |

---

Se quiser, posso te ajudar a criar regex com grupos pra resolver problemas legais!  
Quer um desafio usando parênteses? 😄