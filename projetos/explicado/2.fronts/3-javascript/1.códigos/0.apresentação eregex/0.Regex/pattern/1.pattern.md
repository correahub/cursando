Ótima pergunta, filho! 😄  
Sim! **`pattern`** no HTML **usa regex por trás**. Ou seja:

> **`pattern` é como uma "mini-regex" usada dentro do HTML para validar um campo de formulário.**

---

### 🧪 Exemplo prático:

```html
<form>
  <label>
    Digite apenas letras:
    <input type="text" name="nome" pattern="[A-Za-z]+" required>
  </label>
  <button type="submit">Enviar</button>
</form>
```

Neste exemplo:

- O atributo `pattern="[A-Za-z]+"` é **uma regex**.
    
- Ele **só permite letras maiúsculas ou minúsculas**.
    
- Se a pessoa tentar digitar número ou símbolo, o navegador mostra um erro e **não deixa enviar**.
    

---

### ✅ `pattern` funciona com:

- `<input type="text">`
    
- `<input type="email">` (pode ser usado além da validação padrão)
    
- `<input type="tel">`
    
- `<input type="password">`
    

---

### 🧠 Exemplo com número de telefone:

```html
<input type="tel" pattern="\d{2}\d{5}\d{4}" placeholder="Ex: 11999887766">
```

- Isso só aceita **2 dígitos (DDD) + 5 dígitos + 4 dígitos** (como celular: 11999887766).
    

---

### 🧠 Resumo da mamãe:

|Termo|O que é?|
|---|---|
|`regex`|Uma linguagem usada em qualquer lugar (HTML, JS, Python etc.) para procurar ou validar textos.|
|`pattern`|Um atributo do HTML que usa regex para **validar campos** antes de enviar o formulário.|

---

Ou seja, **`pattern` é uma forma de usar regex dentro do HTML**, com foco em **validação de formulários**. 💪✨

Quer que eu te dê uma lista de padrões prontos pra validar CPF, email, senha, etc?