Aiii coisa mais linda da mamãe! 🥹 Agora vamos fechar esse grupinho fofo dos eventos de mouse com o **`mouseleave`** — o irmãozinho calmo do `mouseout`! 👶🐭🚪

---

## 🍼 O que é o `mouseleave`?

Você já aprendeu o:

- **`mouseover`** → quando o mouse **entra**.
    
- **`mouseout`** → quando o mouse **sai**, **mas pode repetir** se passar por elementos filhos.
    

Agora vem o fofinho:

> **`mouseleave`** é o evento que acontece **quando o mouse sai de um elemento**, **mas NÃO dispara de novo se sair de elementos dentro dele**.

---

### 👶 Comparação simples:

|Evento|Quando dispara?|Se o mouse sai de um filho?|
|---|---|---|
|`mouseout`|Sempre que sai de algo|✅ Sim, dispara de novo|
|`mouseleave`|Só quando sai do elemento todo (o pai)|❌ Não dispara|

---

## 🧸 Exemplo bem lindinho:

```html
<!DOCTYPE html>
<html>
<head>
  <title>Mouseleave do Bebê</title>
</head>
<body>

  <div id="caixa" style="width:200px;height:200px;background-color:lightgreen;">
    Passe o mouse e depois saia 🐭🚶
    <p>Filhinho dentro da caixa</p>
  </div>

  <script>
    let caixa = document.getElementById("caixa");

    // Quando o mouse sai da caixa toda
    caixa.addEventListener("mouseleave", function() {
      alert("O mouse foi embora da caixa! 😢");
    });
  </script>

</body>
</html>
```

---

### 🧠 O que acontece?

- Você passa o mouse na caixa ✅
    
- Sai de dentro da caixa 🚪🐭
    
- **Aparece o alerta** dizendo:
    
    > "O mouse foi embora da caixa!"
    
- ❌ Se você só sair do **texto `<p>` dentro da caixa**, **nada acontece**!
    

---

## 🧪 Testando com `mouseout` e `mouseleave` juntos:

```html
<div id="bloco" style="width:200px;height:200px;background-color:lightcoral;">
  <p>Texto dentro</p>
</div>

<script>
  let bloco = document.getElementById("bloco");

  bloco.addEventListener("mouseout", function() {
    console.log("❗ mouseout aconteceu");
  });

  bloco.addEventListener("mouseleave", function() {
    console.log("✅ mouseleave aconteceu");
  });
</script>
```

### No console (F12):

- **`mouseout`** dispara **várias vezes** se o mouse sai do `<p>`.
    
- **`mouseleave`** só dispara **quando sai da caixa toda**.
    

---

## 💗 Conclusão fofinha da mamãe:

- **`mouseleave`** = o mouse saiu **de verdade**, foi embora da "casinha". 🏠🚶
    
- É calmo, não se assusta com os "filhotes" que estão dentro da caixinha.
    
- Ideal quando você quer reagir **só quando o mouse sai completamente**.
    

---

Mamãe tem tanto orgulho de você! 🥰  
Você já conhece:

✅ `click`  
✅ `mouseover`  
✅ `mouseout`  
✅ `mouseenter`  
✅ `mouseleave`

✨ Já dá pra fazer mágica na tela com o mouse! 🧙‍♂️  
Se quiser criar efeitos, menus, animações ou joguinhos... é só pedir pra mamãe! 👩‍🍼💻💕

Te amo, meu bebê JavaScript! 💖