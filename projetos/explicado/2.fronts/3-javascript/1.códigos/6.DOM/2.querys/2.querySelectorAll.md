Aaaaaah! 😍 Agora sim, meu bebê devzinho chegou no **nível PRO das seleções**:  
✨ **`querySelectorAll`** ✨  
Mamãe vai te explicar **com muito carinho, passo a passo, bem bobinho**, pra você entender tudinho! 🍼💻🌈

---

## 🧠 O que é `querySelectorAll`?

É o **irmão gêmeo** do `querySelector`, mas com um poder especial:

> Ele **não pega só um** elemento…  
> 👉 Ele pega **TODOS** que combinam com o seletor CSS! 💪🍬

---

## 🎈 Diferença entre os dois:

|Comando|Pega quantos?|Exemplo|
|---|---|---|
|`querySelector()`|Só **um** (o primeiro que encontrar)|`document.querySelector(".bolo")`|
|`querySelectorAll()`|**Todos** que combinam com o seletor|`document.querySelectorAll(".bolo")`|

---

## 🍼 Exemplo 1: vários parágrafos

HTML:

```html
<p class="frase">Oi bebê!</p>
<p class="frase">Você é um gênio!</p>
<p class="frase">Continue brilhando!</p>
<button onclick="trocarFrases()">Trocar tudo</button>
```

JavaScript:

```javascript
function trocarFrases() {
  let frases = document.querySelectorAll(".frase");

  for (let i = 0; i < frases.length; i++) {
    frases[i].innerText = "Frase " + (i + 1) + " do bebê!";
  }
}
```

👶 Isso muda o texto de **TODOS os `<p class="frase">`**! Um por um! 🍭🍭🍭

---

## 🍬 Exemplo 2: pintar botões

HTML:

```html
<button class="botao">Botão 1</button>
<button class="botao">Botão 2</button>
<button class="botao">Botão 3</button>
<button onclick="pintar()">Pintar botões</button>
```

JavaScript:

```javascript
function pintar() {
  let botoes = document.querySelectorAll(".botao");

  for (let i = 0; i < botoes.length; i++) {
    botoes[i].style.backgroundColor = "pink";
  }
}
```

👶 Agora todos os botões ficam cor-de-rosa! 🌸

---

## 🧁 Sintaxe mágica:

```javascript
document.querySelectorAll("seletor CSS")
```

🧸 Você pode usar qualquer seletor de CSS aqui:

|Seletor|O que pega|
|---|---|
|`"p"`|Todos os parágrafos|
|`".frase"`|Todos com class "frase"|
|`"#area div"`|Todas as `divs` dentro de `#area`|
|`"input[type='text']"`|Todos os inputs de texto|

---

## 🐣 Coisinha importante:

`querySelectorAll` devolve uma **NodeList**, que parece uma lista:

```javascript
let coisas = document.querySelectorAll(".algo");

coisas[0] // primeiro
coisas[1] // segundo
```

E você pode usar um **`for`** ou até um **`forEach`** (quando você for um bebê mais crescidinho!):

```javascript
coisas.forEach(function(item) {
  item.innerText = "Olá!";
});
```

---

## 💡 Resumindo com muito amor:

|Comando|Serve para...|
|---|---|
|`querySelector("seletor")`|Pegar **um** elemento|
|`querySelectorAll("seletor")`|Pegar **todos os elementos** que combinam com o seletor|
|Formato|Usa o **mesmo estilo do CSS**|
|Retorna|Uma **lista de elementos** (NodeList)|

---

Mamãe está **tão orgulhosa de você** por estar aprendendo tudo isso! 🥹  
Quer um **desafio com `querySelectorAll`** agora, bebê dev? Ou quer usar isso pra **criar uma lista dinâmica**, tipo lista de tarefas, frutas ou brinquedos? 🧸✨