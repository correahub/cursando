Claro, meu amor! 😍  
Agora mamãe vai te ensinar como **transformar um número em um texto (string)** — ou como mamãe chama com carinho: **fazer um número virar palavrinha!** ✨🔢➡️📝

---

### 🧠 O que é isso?

Às vezes, você tem um número (como `10`), mas quer usá-lo **como texto**.  
Por exemplo, pra **mostrar em um alerta**, ou **juntar com outras palavras**, você precisa **transformar esse número em string**.

---

### 🎒 Exemplo ultra básico:

```javascript
let numero = 10;
let texto = String(numero);
alert(texto);
```

Agora, o `alert()` vai mostrar `"10"` — **como texto**, não como número!  
E você pode **juntar com outras palavras**, como:

```javascript
let idade = 12;
let mensagem = "Você tem " + String(idade) + " anos.";
alert(mensagem);
```

Vai aparecer:

> **"Você tem 12 anos."**

---

### 🍭 Outros jeitos de transformar número em texto:

🟢 **1. Usando `String()`** — forma mais comum:

```javascript
let n = 25;
let texto = String(n); // agora é "25"
```

🟢 **2. Usando `.toString()`** — também funciona:

```javascript
let n = 25;
let texto = n.toString(); // agora é "25"
```

Ambos funcionam igual! Você pode usar o que achar mais bonito 😄

---

### 💡 Por que fazer isso?

Porque **textos e números são coisas diferentes**!  
E quando você quer **juntar um número com palavras**, o número **tem que virar texto** primeiro, senão o computador pode se confundir!

---

### 🧚‍♀️ Resumo da mamãe:

|O que você tem|O que você quer|Como faz|
|---|---|---|
|`10` (number)|`"10"` (string)|`String(10)` ou `10.toString()`|
|`preço = 9.9`|`"O preço é 9.9"`|`"O preço é " + String(preço)`|

---

### 🪄 Exemplo final fofo:

```javascript
let idade = prompt("Quantos anos você tem?");
let idadeNumero = parseInt(idade);
let mensagem = "Ano que vem você terá " + String(idadeNumero + 1) + " anos!";
alert(mensagem);
```

---

Se quiser, mamãe também pode te mostrar como transformar **texto em número de novo**, pra fazer continhas! É só dizer! 😄💖