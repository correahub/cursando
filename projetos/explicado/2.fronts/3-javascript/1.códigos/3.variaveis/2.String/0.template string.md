Ai que fofo, meu amorzinho! 🥰  
Agora mamãe vai te ensinar a forma mais **moderna, mágica e fácil** de juntar texto com variáveis no JavaScript!  
Essa magia se chama: **Template String** (ou **Template Literal**) ✨💬💗

---

### 🧠 O que é Template String?

É um jeitinho especial de escrever frases **usando crase** (esse acento aqui: `` ` ``)  
E dentro dela, você coloca variáveis **de um jeito bem fácil**, assim:

```javascript
${nomeDaVariavel}
```

---

### 🧁 Exemplo super simples:

```javascript
let nome = "Lucas";
let idade = 12;

let mensagem = `Olá, ${nome}! Você tem ${idade} anos.`;
alert(mensagem);
```

💬 Vai mostrar:

> **"Olá, Lucas! Você tem 12 anos."**

✅ Sem precisar escrever `"Olá, " + nome + " você tem " + idade + " anos"`  
Agora tudo fica juntinho, bonito e fácil com **crase** e `${}`

---

### 🎒 Como usar:

|Parte mágica|Explicação|
|---|---|
|`` `texto` ``|Usa **crase**, não aspas!|
|`${variavel}`|Coloca variáveis **dentro do texto**|
|Pode colocar contas|Ex: `${idade + 1}` → soma direto!|

---

### ✨ Exemplo com continha:

```javascript
let idade = 12;
let frase = `Ano que vem você terá ${idade + 1} anos!`;
alert(frase);
```

💬 Vai aparecer:

> **"Ano que vem você terá 13 anos!"**

---

### ❗ Importante:

🔸 Você **tem que usar crase**, não aspas:

```javascript
// Certo:
let msg = `Olá, ${nome}`;

// Errado (não funciona):
let msg = "Olá, ${nome}";
```

---

### 🧚‍♀️ Resumo da mamãe:

|Coisa mágica|O que faz?|
|---|---|
|`` `texto` ``|Marca o início e fim da frase (com crase)|
|`${variavel}`|Coloca a variável dentro da frase|
|Tudo junto e bonito!|Muito melhor do que usar vários `+`|

---

Se quiser, mamãe pode te mostrar como fazer isso com **`prompt()`**, pegando o nome da pessoa e já dando boas-vindas com uma mensagem linda!  
Quer, meu príncipe do código? 😄👑💻