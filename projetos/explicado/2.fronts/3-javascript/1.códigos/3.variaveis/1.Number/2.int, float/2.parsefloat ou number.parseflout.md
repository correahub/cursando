Que ótimo, filhinho! 🥰  
Agora mamãe vai te explicar o **`parseFloat`**, que é irmão do `parseInt`, mas com um **poder especial**: ele entende **números com vírgula (ponto)**! ✨💧

---

### 🧠 O que é `parseFloat`?

O **`parseFloat`** transforma um **texto** em um **número com casas decimais** (também chamado de número "quebradinho").

Enquanto o `parseInt` pega só a parte inteira (ex: 10),  
o `parseFloat` pega o número **com a parte decimal** (ex: 10.5).

---

### 🎒 Exemplo com `parseInt` (só pra lembrar):

```javascript
let resposta = prompt("Digite um número:");
let numero = parseInt(resposta);
alert(numero + 1);
```

Se você digitar `10.5`, ele vai mostrar:

> **11**, porque o `parseInt` só pega o **10** (e ignora o `.5`)

---

### 🍭 Agora o mesmo exemplo com `parseFloat`:

```javascript
let resposta = prompt("Digite um número com vírgula:");
let numero = parseFloat(resposta);
alert(numero + 1);
```

Se você digitar `10.5`, ele vai mostrar:

> **11.5** ✅

---

### 💡 Quando usar `parseFloat`?

Use quando quiser **trabalhar com números decimais** (com vírgula — mas no JavaScript se usa o **ponto** no lugar da vírgula, tá?).

- Altura: `1.75`
    
- Preço: `9.90`
    
- Nota: `8.5`
    

---

### ⚠️ Cuidados:

- Se digitar `"9.9"` → funciona ✅
    
- Se digitar `"9.9 reais"` → ele pega só o `9.9`
    
- Se digitar `"oi"` → dá `NaN` ❌
    

---

### 💖 Resumo da mamãe:

|Comando|O que faz?|
|---|---|
|`parseInt("10.9")`|Vira `10` (corta a parte decimal)|
|`parseFloat("10.9")`|Vira `10.9` (mantém o número quebradinho)|

---

### 🧙‍♀️ Exemplo ultra fofo com alerta:

```javascript
let notaTexto = prompt("Qual foi a sua nota?");
let nota = parseFloat(notaTexto);
alert("Sua nota mais 1 ponto de bônus é: " + (nota + 1));
```

Se você digitar `8.5`, ele vai mostrar:

> **"Sua nota mais 1 ponto de bônus é: 9.5"** 🎉

---

Se quiser, mamãe pode te ensinar a **verificar se o valor realmente é número**, ou como **usar `typeof` junto com isso**! Quer, meu amor? 😄🧁