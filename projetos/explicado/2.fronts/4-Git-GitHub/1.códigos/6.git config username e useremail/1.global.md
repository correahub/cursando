
### 🧒 Imagine que você vai escrever cartas para seus amigos.

Cada vez que você escreve uma carta (como se fosse uma **mudança no código com o Git**), você precisa **assinar** a carta com seu **nome** e **e-mail**, para que todos saibam **quem escreveu**.

O Git funciona do mesmo jeitinho!

### ✉️ O que é `git config user.name` e `git config user.email`?

Quando você usa o Git para salvar suas mudanças no código, ele quer saber:

- **Quem é você?** 👉 Isso é o `user.name`
    
- **Qual é o seu e-mail?** 👉 Isso é o `user.email`
    

Assim, cada vez que você salvar uma mudança (isso se chama um **commit**), o Git coloca seu nome e e-mail lá, como uma assinatura.

---

### 🛠 Como fazer isso?

Você precisa dizer isso pro Git só uma vez (ou quando quiser mudar). Aqui está como você faz:

```bash
git config --global user.name "Maria Programadora"
git config --global user.email "maria@exemplo.com"
```

#### 🧠 Vamos entender essa frase:

- `git config`: é o comando para configurar alguma coisa no Git.
    
- `--global`: significa que vale para **todos os projetos no seu computador**.
    
- `user.name`: é onde você coloca **seu nome**.
    
- `user.email`: é onde você coloca **seu e-mail**.
    

---

### 🎁 Exemplo divertido:

Vamos imaginar que você é a **Maria** e está brincando de construir um castelo com peças (seu projeto). A cada vez que você coloca uma peça nova (uma mudança), você escreve:

📜 “Hoje eu adicionei a torre do castelo. — Maria ([maria@exemplo.com](mailto:maria@exemplo.com))”

Assim, todos sabem que foi você que fez isso. Se um amigo seu abrir esse projeto, ele vai ver: **"Ah, foi a Maria que fez essa parte!"**

---

### ✅ Dica final:

Você só precisa configurar isso uma vez e o Git sempre vai lembrar quem é você!

---

Se quiser, podemos fazer juntos agora com seus dados. Quer tentar? 😊