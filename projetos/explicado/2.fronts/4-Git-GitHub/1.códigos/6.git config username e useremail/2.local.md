
---

### 🧁 Explicando como para uma criança:

Imagine que você está brincando com vários tipos de brinquedos. Num projeto, você está montando um castelo 🏰, e em outro, está construindo uma nave espacial 🚀.

Você quer **assinar o castelo como "Maria do Castelo"** e a **nave como "Maria Astronauta"**. Cada projeto tem uma assinatura diferente!

O Git deixa você fazer isso!

---

### ⚙️ Como configurar localmente:

Você precisa **estar dentro da pasta do projeto** e depois digitar:

```bash
git config user.name "Maria do Castelo"
git config user.email "castelo@maria.com"
```

Isso **só vale para aquele projeto**, e não muda os outros.

---

### 💡 Qual a diferença entre global e local?

|Tipo de configuração|Comando usado|Vale para...|
|---|---|---|
|Global|`git config --global`|Todos os projetos no seu computador|
|Local|`git config` (sem `--global`)|Só o projeto atual|

---

### 🧸 Exemplo prático:

1. Você criou um projeto chamado `meu-castelo`:
    
    ```bash
    cd meu-castelo
    ```
    
2. E agora quer usar um nome diferente **só nesse projeto**:
    
    ```bash
    git config user.name "Maria do Castelo"
    git config user.email "castelo@maria.com"
    ```
    
3. Se você for para outro projeto depois, o nome e e-mail vão continuar os mesmos **de antes** (ou vão seguir o global se você tiver configurado um).
    

---

Se quiser, posso te ajudar a fazer isso agora, passo a passo! Quer tentar? 😊