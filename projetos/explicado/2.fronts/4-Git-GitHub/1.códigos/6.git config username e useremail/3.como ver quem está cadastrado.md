Ótima pergunta! 😄  
Se você quer ver **qual usuário e e-mail estão registrados no Git**, é bem simples fazer isso pelo **prompt/terminal** (bash). Vamos lá, passo a passo:

---

## 👣 PASSO A PASSO:

### ✅ 1. **Ver as configurações do Git no terminal**

Digite este comando no **bash** (ou Git Bash):

```bash
git config --list
```

Esse comando mostra **todas as configurações do Git** ativas naquele momento, incluindo:

- `user.name` → o nome do usuário
    
- `user.email` → o e-mail usado nos commits
    

🔍 Exemplo de saída:

```
user.name=Maria Silva
user.email=maria@example.com
```

---

### ✅ 2. **Ver uma configuração específica**

Se você quiser ver **só o nome**, use:

```bash
git config user.name
```

Ou, para ver **só o e-mail**, use:

```bash
git config user.email
```

---

### ✅ 3. **Ver o arquivo diretamente no Git Bash (opcional)**

Se você quiser **abrir e ler o arquivo `config` diretamente**, ele normalmente está dentro da pasta oculta `.git` do seu repositório.

#### Comando para isso:

```bash
cat .git/config
```

> Isso mostra o conteúdo do arquivo `config` dentro da pasta `.git` do projeto em que você está.

📌 Atenção: isso mostra **as configurações locais do projeto** (ou seja, só desse repositório).

---

### 🧠 Extra: Tipos de configuração do Git

|Tipo|Onde é salvo|Comando para ver|
|---|---|---|
|**Global**|Para todos os projetos do usuário|`git config --global --list`|
|**Local**|Só para o projeto atual (`.git/config`)|`git config --local --list`|
|**System**|Para todos os usuários do sistema|`git config --system --list`|

---

Se quiser, posso te mostrar como **editar ou mudar o nome e o e-mail também**, de forma simples! Deseja isso? 😊