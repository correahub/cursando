
---

### 🌟 O que é o estágio **modified**?

Quando um arquivo está em estado **modified**, isso quer dizer que:

👉 O Git **já está cuidando do arquivo** (então ele é **tracked**)  
👉 Mas você **mudou alguma coisa** dentro dele, e o Git percebeu essa mudança!

Em outras palavras, é como quando você tem um brinquedo que já está guardado no seu caderno (o Git já conhece), mas você fez uma alteração nele (como pintar ou trocar uma peça) – o Git **anota que o brinquedo foi modificado!** 🎨

---

### 💡 Exemplo prático

💻 Suponha que você já tem um arquivo chamado `poema.txt` no seu projeto.

Ele tem esse conteúdo:

```
Rosas são vermelhas,
Violetas são azuis.
```

E você já adicionou ao Git:

```bash
git add poema.txt
git commit -m "Adiciona o poema"
```

Agora ele está **tracked**.

---

### 🎨 Vamos modificar!

Você abre o arquivo e muda o conteúdo para:

```
Rosas são vermelhas,
Violetas são azuis.
O céu é tão bonito,
Brilha como um cristal.
```

---

### 🚀 O que o Git fala?

Quando você faz:

```bash
git status
```

O Git vai mostrar algo assim:

```
modified:   poema.txt
```

Isso quer dizer:

> "Ei! Eu conheço esse arquivo `poema.txt` porque ele já estava guardado. Mas eu percebi que você mudou ele desde a última vez que eu o anotei!"

---

### 🔎 Resumo

✅ **modified** = O arquivo **já era tracked** e agora você fez uma modificação nele.  
✅ O Git **anota que houve uma mudança**, mas você ainda não contou para ele se quer guardar essa mudança (você faz isso com `git add` e `git commit`!).

---

### 📝 Recapitulando os passos

1️⃣ Você cria ou já tem o arquivo e ele está no Git (tracked).  
2️⃣ Você muda alguma coisa dentro do arquivo.  
3️⃣ O Git vê isso e marca o arquivo como **modified**.

---
