Perfeito! Agora vamos juntar as duas coisas que você aprendeu:

- `git log --oneline`
    
- `git log -1`, `-2`, etc.
    

E explicar o que acontece quando usamos **juntos** assim:

```bash
git log --oneline -1
```

---

### 🎯 O que é `git log --oneline`?

É como pedir para o Git:

> “Mostre o histórico de forma **bem curtinha**, só uma linha para cada mudança!”

Exemplo:

```bash
a1b2c3d Adicionei receita de brigadeiro  
e4f5g6h Adicionei receita de pizza  
x7y8z9a Adicionei receita de bolo
```

---

### 🧠 O que é `git log --oneline -1`?

Agora você está dizendo:

> “Me mostre **apenas o último commit**, e **em uma linha só**.”

Então a saída seria assim:

```
a1b2c3d Adicionei receita de brigadeiro
```

---

### 🍕 Outros exemplos:

#### `git log --oneline -2`

Vai mostrar as **duas últimas mudanças**, cada uma em **uma linha**:

```
a1b2c3d Adicionei receita de brigadeiro  
e4f5g6h Adicionei receita de pizza
```

#### `git log --oneline -3`

Vai mostrar as **três últimas**:

```
a1b2c3d Adicionei receita de brigadeiro  
e4f5g6h Adicionei receita de pizza  
x7y8z9a Adicionei receita de bolo
```

---

### ✅ Resumo para criança:

- `--oneline` = modo **resumido**, cabe tudo numa linha!
    
- `-1`, `-2`, `-3` = quantos **passos do diário** você quer ver.
    

Então `git log --oneline -2` é como dizer:

> “Git, me dá **os 2 últimos bilhetes**, mas escreve **bem rápido e curtinho**!”

---
