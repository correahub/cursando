

### 🧩 O que é o **hash ID** de um commit?

Quando você faz um **commit** no Git (ou seja, guarda uma versão do seu trabalho com uma mensagem), o Git dá para esse commit um **nome super especial** e único, chamado de **hash ID**.

Esse nome é um monte de letras e números esquisitos, como por exemplo:

```
a1b2c3d4e5f67890123456789abcdef12345678
```

🎯 Esse é o **hash ID do commit**.

---

### 🧠 Mas por que o Git faz isso?

Imagine que você tem um **diário mágico** que guarda tudo o que você faz. Mas você não quer chamar as páginas de "Página 1", "Página 2", porque isso pode dar confusão se você mudar a ordem.  
Em vez disso, o diário dá a **cada página um nome secreto**, gerado a partir do conteúdo daquela página. Isso garante que **nenhuma página terá o mesmo nome**.

No Git, esse nome secreto é o **hash** — ele serve para:

- 📌 Identificar aquele commit de forma única.
    
- 🕵️‍♂️ Permitir que você volte exatamente para aquele ponto no tempo.
    
- 🔗 Conectar commits uns aos outros, como uma corrente.
    

---

### 🔍 Exemplo prático

Você faz este commit:

```bash
git commit -m "Adicionei a parte da montanha"
```

E o Git mostra isso:

```
[main abc1234] Adicionei a parte da montanha
```

Esse `abc1234` é **o comecinho do hash ID**. O nome completo pode ser algo assim:

```
abc1234def56789abcde0000f111aabbccddeeff
```

💡 O Git mostra só os primeiros 7 caracteres porque isso **já é suficiente** para identificar o commit na maioria dos projetos.

---

### 🧪 O hash muda se eu mudar o conteúdo?

Sim! Se você mudar **qualquer coisa** no commit (como os arquivos ou a mensagem), o Git gera um hash **totalmente diferente**, mesmo que só mude uma letra.

---

### 🎁 Resumo para lembrar:

|Termo|Explicação simples|
|---|---|
|Hash ID|Um "nome mágico" único feito de letras e números|
|Usado para|Identificar commits e garantir que não haja confusão|
|Exemplo|`a1b2c3d4e5f6789012...`|
|Muda quando|O conteúdo ou a mensagem do commit muda|

---
