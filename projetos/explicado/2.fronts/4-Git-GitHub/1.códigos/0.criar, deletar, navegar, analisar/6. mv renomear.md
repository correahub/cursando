
---

## 🎨 Vamos imaginar que você está organizando seus **desenhos em pastas** no computador.

Você tem uma **pasta chamada `meus-desenhos`**, e quer **trocar o nome dela para `desenhos-legais`**.

---

## 🧠 O que é o comando `mv`?

O `mv` é como dizer para o computador:

> “**Mova ou renomeie** esse arquivo ou pasta.”

É a **ferramenta mágica que você usa para mudar nomes ou lugares de coisas** no computador.

---

### 🟢 Quando você quer **mudar o nome de uma pasta ou arquivo**, o `mv` funciona assim:

```bash
mv nome_antigo nome_novo
```

---

## 🧪 Exemplo 1: Renomear uma **pasta**

Você tem uma pasta chamada `meus-desenhos`, mas acha esse nome chato.  
Quer mudar para `desenhos-legais`.

Então você escreve:

```bash
mv meus-desenhos desenhos-legais
```

💬 Isso quer dizer:

> "Ei computador, pega a pasta chamada `meus-desenhos` e mude o nome dela para `desenhos-legais`!"

---

## 🧪 Exemplo 2: Renomear um **arquivo**

Você tem um arquivo chamado `gato.txt`, e quer mudar o nome para `gato-fofo.txt`.

Então você escreve:

```bash
mv gato.txt gato-fofo.txt
```

💬 Isso quer dizer:

> "Mude o nome do arquivo `gato.txt` para `gato-fofo.txt`."

---

## 🧪 Exemplo 3: Mover um arquivo para dentro de uma pasta

Agora vamos dizer que você tem:

- um arquivo chamado `gato.txt`
    
- uma pasta chamada `desenhos-legais`
    

E você quer **colocar o `gato.txt` dentro da pasta `desenhos-legais`**.

Você escreve:

```bash
mv gato.txt desenhos-legais/
```

💬 Isso quer dizer:

> "Pegue o `gato.txt` e coloque ele dentro da pasta `desenhos-legais`."

---

## 🛑 Atenção: se o segundo nome **não for uma pasta existente**, o `mv` entende que você quer **renomear**.

---

## 🏁 Recapitulando com um resumo simples:

|O que você quer fazer|Comando usado|
|---|---|
|Renomear uma pasta|`mv nome_antigo nome_novo`|
|Renomear um arquivo|`mv nome_antigo.txt novo_nome.txt`|
|Mover um arquivo para uma pasta|`mv arquivo.txt nome-da-pasta/`|
|Mover e renomear ao mesmo tempo|`mv arquivo.txt nome-da-pasta/novo.txt`|

---

Se quiser, posso montar um exercício prático pra você treinar isso com pastas e arquivos falsos no seu terminal. Quer?