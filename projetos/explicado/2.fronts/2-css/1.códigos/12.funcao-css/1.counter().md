# A função counter() é usada para mostrar o valor do contador atual. 
























---

## 🔢 O que é `counter()`?

Pense no `counter()` como uma **janelinha que mostra o número do contador** 👀

O contador:

- já foi **criado** (`counter-reset`)
    
- já foi **aumentado** (`counter-increment`)
    

Agora o `counter()` serve apenas para:  
👉 **mostrar o número na tela**

---

## 🧠 O que ele faz na prática?

Quando você escreve:

```css
content: counter(item);
```

O CSS entende:

> “Mostre o valor atual do contador chamado `item`.”

📺 Ele só exibe, não muda nada.

---

## 📍 Onde o `counter()` pode ser usado?

⚠️ **Apenas dentro da propriedade `content`**  
Normalmente com:

- `::before`
    
- `::after`
    

Exemplo:

```css
li::before {
  content: counter(item);
}
```

---

## 🧩 Exemplo mental simples

Imagine que o contador está assim:

```
item = 3
```

Se você usar:

```css
content: counter(item);
```

🖥️ O que aparece na tela:

```
3
```

Simples assim 😄

---

## 🎨 Mudando o formato do número

Você pode escolher **como o número aparece**:

```css
content: counter(item, upper-roman);
```

Resultado:

```
I, II, III
```

Alguns formatos comuns:

- `decimal` → 1, 2, 3
    
- `lower-alpha` → a, b, c
    
- `upper-alpha` → A, B, C
    
- `lower-roman` → i, ii, iii
    
- `upper-roman` → I, II, III
    

---

## 🔗 Juntando texto + número

```css
li::before {
  content: "Item " counter(item) ": ";
}
```

🖥️ Resultado:

```
Item 1: Texto
Item 2: Texto
```

---

## ❌ O que `counter()` NÃO faz

❌ Não cria contador  
❌ Não incrementa  
❌ Não reinicia

Ele **só mostra o valor atual** 👀

---

## 🧠 Resumo mental rápido

> **counter()** = mostra o número do contador 🔢

---

## 📘 Definição técnica (simples)

> **`counter()`** é uma **função CSS** usada dentro da propriedade `content` para **exibir o valor atual de um contador**, com possibilidade de definir seu formato numérico. 🔢🎨

Se quiser, o próximo passo natural é:  
👉 `counters()` (contador aninhado) 🌳  
👉 exemplo real de capítulos `1.2.3` 📖  
👉 erro comum usando `counter()`

É só falar 😄🚀