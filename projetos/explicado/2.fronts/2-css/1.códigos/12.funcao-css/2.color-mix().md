# A função color-mix()`mistura cores em uma proporção definida.

























---

## 🎨 O que é `color-mix()` no CSS?

**`color-mix()`** é uma **função CSS** usada para **misturar duas (ou mais) cores** e criar **uma nova cor** automaticamente.

👉 É como misturar tintas 🎨

- azul + amarelo → verde
    
- vermelho + branco → rosa
    

No CSS, quem faz essa mistura é o **`color-mix()`**.

---

## 🧠 Definição técnica (simples)

**`color-mix()`** é uma **função CSS** que **combina cores em uma proporção definida**, retornando **uma nova cor**. 🎨✨

---

## 🧱 Estrutura básica

```css
color-mix(in <color-space>, <cor1> <porcentagem>, <cor2> <porcentagem>)
```

Vamos quebrar isso 👇

---

## 1️⃣ `in <color-space>` → espaço de cor

Define **como as cores serão misturadas**.

Os mais comuns:

- `srgb` (mais usado)
    
- `hsl`
    
- `lab`
    
- `lch`
    

Exemplo:

```css
color-mix(in srgb, red, blue)
```

👉 Se você não souber qual usar, **use `srgb`** 👍

---

## 2️⃣ As cores 🎨

Você pode usar:

- nomes (`red`, `blue`)
    
- hex (`#ff0000`)
    
- `rgb()`
    
- `hsl()`
    
- variáveis CSS
    

Exemplo:

```css
color-mix(in srgb, #ff0000, #0000ff)
```

---

## 3️⃣ Porcentagens (%) 📊

As porcentagens dizem **quanto de cada cor entra na mistura**.

```css
color-mix(in srgb, red 70%, blue 30%)
```

👉 Resultado: mais vermelho que azul.

🔹 Se você **não colocar porcentagem**, o CSS assume **50% / 50%** automaticamente.

---

## 📌 Exemplos práticos

### 🔹 Mistura simples (50% / 50%)

```css
background-color: color-mix(in srgb, red, blue);
```

➡️ Roxo 💜

---

### 🔹 Mistura com porcentagem

```css
background-color: color-mix(in srgb, red 80%, blue 20%);
```

➡️ Vermelho puxando para roxo 🔴🟣

---

### 🔹 Usando variável CSS

```css
:root {
  --cor-principal: blue;
}

div {
  background-color: color-mix(in srgb, var(--cor-principal), white 40%);
}
```

➡️ Azul mais claro 🔵✨

---

## 🎯 Uso comum do `color-mix()`

Muito usado para:

- hover mais claro ou mais escuro
    
- estados de botão
    
- sombras suaves
    
- temas dinâmicos
    

Exemplo de hover 👇

```css
button {
  background: blue;
}

button:hover {
  background: color-mix(in srgb, blue, white 20%);
}
```

➡️ O botão clareia suavemente 😍

---

## ⚠️ Observações importantes

🔹 Nem todos os navegadores antigos suportam  
🔹 Funciona melhor com **CSS moderno**  
🔹 Excelente alternativa a `rgba()` em muitos casos

---

## 🧠 Resumo final (bem curtinho)

- 🎨 `color-mix()` mistura cores
    
- 📊 Você controla a quantidade de cada cor
    
- 🧱 Retorna **uma nova cor**
    
- 💡 Ótimo para efeitos visuais modernos
    

---

Se quiser, posso:  
👉 comparar `color-mix()` vs `rgba()`  
👉 mostrar exemplos reais de UI  
👉 explicar os **color-spaces** (`srgb`, `lab`, `lch`)

Só dizer 😄🎨