## ✅ `:valid` — “Tudo certo aqui!”

Algumas caixinhas têm regras. Por exemplo:

```html
<input type="email" required>
```

Essa caixinha quer que você digite **um e-mail**. Se você digitar **algo válido**, tipo:

```
meuemail@teste.com
```

Ela fica **válida**! Aí o CSS pode reagir:

```css
input:valid {
  border: 2px solid green;
}
```

📌 **O que acontece?**  
Se você digitar um e-mail de verdade, a borda vai ficar **verde**, mostrando que está tudo ok.
