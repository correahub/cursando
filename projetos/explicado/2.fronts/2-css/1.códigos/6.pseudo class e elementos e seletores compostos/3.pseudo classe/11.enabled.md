Claro! Agora vamos aprender a pseudo-classe **`:enabled`**, explicada como se fosse para uma criança aprender brincando! 😄

---

### 🧒 Imagina isso:

Você tem um formulário com várias **caixinhas para preencher**. Algumas estão **acesas e prontas para usar**, e outras estão **apagadas ou travadas** (não dá pra escrever nelas).

> A pseudo-classe `:enabled` serve para **detectar quais campos estão ligados (ativos)** — ou seja, prontos para o usuário digitar ou clicar! ✅

---

### 💡 O que é `:enabled`?

`**:enabled**` é uma **pseudo-classe CSS** que **seleciona todos os elementos de formulário que estão ativados**, ou seja, que **podem ser usados normalmente**.

Funciona com:

- `<input>`
    
- `<textarea>`
    
- `<select>`
    
- `<button>`
    

---

### 📦 Exemplo simples:

HTML:

```html
<input type="text" placeholder="Nome" class="campo1">
<input type="text" placeholder="Desativado" class="campo2" disabled>
```

CSS:

```css
input:enabled {
  border: 2px solid green;
  background-color: #eaffea;
}
```

### ✅ O que vai acontecer?

- O primeiro campo (que está **ativado**) vai ganhar **borda verde** e **fundo verdinho**.
    
- O segundo campo (com `disabled`) **não muda nada**, porque ele está **desativado**.
    

---

### 🤔 O que é “desativado”?

Quando você escreve `disabled` em um campo, ele **não pode ser clicado nem preenchido**. É como se estivesse travado.

---

### 🎨 Exemplo com botão:

HTML:

```html
<button>Enviar</button>
<button disabled>Carregando...</button>
```

CSS:

```css
button:enabled {
  background-color: blue;
  color: white;
}
```

O botão “Enviar” fica azul com letras brancas.  
O botão “Carregando...” não muda, pois está desativado.

---

### 🧠 Dica final:

O `:enabled` é ótimo para **estilizar os campos que o usuário pode usar**.

Você pode combinar com `:disabled` para dar **estilos diferentes** para campos ativos e travados, como:

```css
input:enabled {
  background-color: white;
}
input:disabled {
  background-color: lightgray;
  color: gray;
}
```

---

Se quiser, posso mostrar um exemplo de formulário onde o botão **só ativa quando o usuário digita algo**, usando `:enabled` e `:disabled` juntos com um pouco de lógica. Quer ver? 😄