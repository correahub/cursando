# :enabled` é uma pseudo-class que aplica estilos nos elementos interativos que não estão desativados.

---

# 🟢 O que é **`:enabled`** no CSS?

**`:enabled` é uma pseudo-class que seleciona elementos que estão habilitados**, ou seja, aqueles que **podem ser usados**, clicados ou preenchidos.

Ela funciona principalmente com elementos de formulário, como:

- `<input>`
    
- `<button>`
    
- `<select>`
    
- `<textarea>`
    

---

## 📘 Explicação simples (igual às suas definições):

**Pseudo-class `:enabled` é um seletor que pega elementos que estão ativados e prontos para usar.** 🟢✨

---

## 🧒 Explicando como para uma criança:

Imagina que você tem vários botões e caixas de texto.  
Alguns estão “ligados” e você pode clicar ou escrever neles,  
e outros estão “desligados” (cinzinha), que você não pode usar.

O `:enabled` é como o CSS falando:

🟢 “Esse aqui está ligado! Posso estilizar!”

E só vai aplicar o estilo nos elementos que **não estão desabilitados**.

---

## 🧪 Exemplo simples:

### 🔗 HTML:

```html
<input type="text" placeholder="Digite algo">
<input type="text" placeholder="Desabilitado" disabled>

<button>Botão normal</button>
<button disabled>Botão desabilitado</button>
```

### 🎨 CSS:

```css
input:enabled,
button:enabled {
  border: 2px solid green;
  background: #eaffea;
}
```

👉 Apenas os elementos que você **pode usar** ficam verdinhos.

---
