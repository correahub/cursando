## 🌟 Primeiro: o que é uma **pseudo-classe**?

É como se fosse uma **regra especial** no CSS que muda o estilo de alguma coisa **quando algo acontece** com ela.

---

## 🔍 `:focus` — “Estou prestando atenção!”

Quando você **clica em uma caixinha de texto**, o navegador entende:  
📣 “Ei, essa caixinha está com o **foco** agora! A criança está digitando aqui!”

```html
<input type="text" placeholder="Digite seu nome">
```

```css
input:focus {
  border: 2px solid blue;
}
```

📌 **O que acontece?**  
Quando você clicar na caixinha, ela vai ganhar uma **borda azul**!  
Quando você clicar em outro lugar, ela perde o foco, e a borda azul some.

---

Beleza 😄 vamos imaginar o seguinte:  
você tem **vários brinquedos em uma prateleira**, e quer brincar com eles **na ordem certa**.  
O navegador faz a mesma coisa quando alguém **usa o teclado (com a tecla Tab)** para navegar pelos elementos de uma página — como botões, links, campos de texto etc.

Agora entra o **`tabindex`**, que é como se fosse um **número que diz a ordem dos brinquedos** (ou elementos) que o navegador vai visitar quando a pessoa aperta a tecla **Tab**.

Vamos ver os casos do **`tabindex="0"`** e **`tabindex="1"`**, explicando como se fosse uma historinha 👇

---

### 🎯 `tabindex="0"` — “entra na fila normal”

Pensa que o `tabindex="0"` é como dizer:

> “Ei, eu quero brincar também! Mas deixa eu entrar na fila **junto com os outros brinquedos normais**, no meu lugar certinho.”

💡 Significa:

- O elemento **entra na ordem padrão** da página, junto com os outros que já podem ser focados (como botões e links).
    
- Quando você aperta **Tab**, ele será focado **no fluxo normal**.
    
- Você pode dar `tabindex="0"` em um elemento que normalmente **não receberia foco** (como uma `<div>`), pra permitir que ele receba foco sem mudar a ordem natural.
    

🧩 Exemplo:

```html
<div tabindex="0">Sou uma div, e agora posso ser focada!</div>
<button>Botão</button>
<a href="#">Link</a>
```

👉 Quando você aperta **Tab**, o foco vai:

1. Div (porque está antes na página e tem tabindex=0)
    
2. Botão
    
3. Link
    

---

### 🚀 `tabindex="1"` — “quero ser o primeiro da fila!”

Agora, o `tabindex="1"` é como se o brinquedo dissesse:

> “Ei! Eu quero brincar primeiro, antes de todo mundo!”

💡 Significa:

- O elemento com `tabindex="1"` vai ser **focado antes de qualquer outro**, **mesmo que apareça depois** no código.
    
- Se tiver outro com `tabindex="2"`, ele vem depois, e assim por diante.
    
- Mas isso **pode atrapalhar** a navegação natural da página, então é bom **não abusar**.
    

🧩 Exemplo:

```html
<button>Botão 1</button>
<button tabindex="1">Botão 2 com tabindex 1</button>
<button>Botão 3</button>
```

👉 Quando você aperta **Tab**, o foco vai:

1. Botão 2 (porque tem tabindex=1)
    
2. Botão 1 (segue a ordem normal depois)
    
3. Botão 3
    

---

### ⚠️ Dica importante

Usar `tabindex="1"`, `tabindex="2"` etc. **não é muito recomendado**, porque pode **confundir os usuários** e os leitores de tela.  
O mais comum e acessível é usar só:

- `tabindex="0"` (para incluir um elemento na ordem natural)
    
- `tabindex="-1"` (para permitir foco apenas por JavaScript, não pelo Tab)
    

---

### 🌈 Resumo fácil:

|Valor|O que faz|Explicação simples|
|---|---|---|
|`0`|entra na fila normal|“Quero ser focado junto com os outros.”|
|`1` (ou mais)|muda a ordem natural|“Quero ser o primeiro da fila!”|
|`-1`|não entra na fila do Tab|“Só quero foco se alguém me chamar (JavaScript).”|

---

Quer que eu te mostre um **exemplo interativo** (com HTML e CSS) pra você testar o `tabindex` e ver o que acontece quando aperta **Tab**?