 # **CSS Counter** é usado para **controlar e numerar elementos**.





















---

## 🧮 O que é `counter` no CSS?

Pense em um **contador de números automático**, igual aqueles que contam:

👉 1, 2, 3, 4…

No CSS, o **counter** serve exatamente para isso:  
👉 **criar números automáticos usando apenas CSS**, sem JavaScript.

Ele é muito usado para:

- Numerar listas personalizadas 📋
    
- Criar capítulos, seções, passos, rankings 🏆
    
- Mostrar números antes de textos
    

---

## 🧠 Como o contador funciona?

O contador funciona em **3 etapas**, como um joguinho 🎮:

---

### 1️⃣ `counter-reset` → cria o contador

Aqui você **cria** o contador e diz em qual número ele começa.

```css
body {
  counter-reset: contador;
}
```

🧠 Isso significa:

> “CSS, crie um contador chamado `contador` começando do zero.”

---

### 2️⃣ `counter-increment` → aumenta o número

Agora você diz:

> “Toda vez que esse elemento aparecer, aumente o contador em +1”

```css
li {
  counter-increment: contador;
}
```

📈 Cada `<li>` soma +1 no contador.

---

### 3️⃣ `counter()` → mostra o número na tela

Para **exibir o número**, usamos `::before` ou `::after` com `content`.

```css
li::before {
  content: counter(contador) ". ";
}
```

👀 Resultado visual:

```
1. Item
2. Item
3. Item
```

Tudo automático 🤯✨

---

## 🔗 Exemplo completo simples

### HTML

```html
<ul>
  <li>Maçã</li>
  <li>Banana</li>
  <li>Laranja</li>
</ul>
```

### CSS

```css
ul {
  counter-reset: frutas;
}

li {
  counter-increment: frutas;
}

li::before {
  content: counter(frutas) " - ";
}
```

🖥️ Resultado:

```
1 - Maçã
2 - Banana
3 - Laranja
```

---

## 🔢 Tipos de números (formatos)

Você pode mudar o estilo do número:

```css
content: counter(frutas, upper-roman);
```

Alguns formatos:

- `decimal` → 1, 2, 3
    
- `lower-alpha` → a, b, c
    
- `upper-alpha` → A, B, C
    
- `lower-roman` → i, ii, iii
    
- `upper-roman` → I, II, III
    

📚✨

---

## ⚠️ Importante saber

- `counter` **não funciona sozinho**
    
- Ele **sempre precisa** de:
    
    - `counter-reset`
        
    - `counter-increment`
        
    - `counter()`
        

É como um trio 🎶 — se faltar um, não funciona.

---

## 🧠 Definição técnica (simples)

> **CSS Counter** é um mecanismo do CSS usado para **criar e controlar numeração automática de elementos**, utilizando `counter-reset`, `counter-increment` e a função `counter()` para exibir valores numéricos no conteúdo. 🔢🎨

Se quiser, posso te mostrar:

- `counters()` (contador aninhado) 🌳
    
- Exemplo real de capítulos 📖
    
- Diferença entre `counter()` e `counters()`
    

É só dizer 😄🚀