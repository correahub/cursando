## 🎯 O que é `text-decoration`?

`text-decoration` é uma **propriedade CSS** usada para **adicionar efeitos visuais ao texto**, como **sublinhado**, **riscado**, **linha acima do texto**, entre outros.

Ela **controla a aparência da linha decorativa** do texto.

---

## 🧱 Estrutura da propriedade

A propriedade `text-decoration` pode ser usada de forma simples ou **em partes separadas**:

### ✅ Forma resumida (shorthand):

```css
text-decoration: underline red wavy;
```

Aqui, você está dizendo:

- underline → tipo da linha (sublinhado)
    
- red → cor da linha
    
- wavy → estilo da linha (ondulada)
    

Mas você também pode usar cada parte separadamente, veja abaixo:

---

## 🔍 Partes da `text-decoration` separadas

### 1. `text-decoration-line`

Define **onde a linha aparece no texto**.

```css
text-decoration-line: underline; /* Sublinhado */
text-decoration-line: overline; /* Linha acima */
text-decoration-line: line-through; /* Riscado */
text-decoration-line: none; /* Remove a decoração */
```

Você pode combinar:

```css
text-decoration-line: underline overline;
```

---

### 2. `text-decoration-color`

Define a **cor da linha decorativa**.

```css
text-decoration-color: blue;
text-decoration-color: #FF0000; /* vermelho em hexadecimal */
```

> 💡 Mesmo que o texto seja preto, a linha pode ser de outra cor!

---

### 3. `text-decoration-style`

Define o **estilo da linha** (como ela parece).

```css
text-decoration-style: solid;    /* linha reta padrão */
text-decoration-style: double;   /* duas linhas */
text-decoration-style: dotted;   /* pontilhada */
text-decoration-style: dashed;   /* tracejada */
text-decoration-style: wavy;     /* ondulada */
```

---

### 4. `text-decoration-thickness` (opcional, mais moderno)

Controla a **espessura** da linha (nem todos os navegadores suportam 100%).

```css
text-decoration-thickness: 2px;
text-decoration-thickness: thick;
```

---

## 📦 Exemplo completo

```css
p {
  text-decoration-line: underline;
  text-decoration-color: red;
  text-decoration-style: wavy;
  text-decoration-thickness: 2px;
}
```

Ou com shorthand:

```css
p {
  text-decoration: underline red wavy 2px;
}
```

---

## 📌 Usos comuns no dia a dia

|Situação|CSS|
|---|---|
|Link com sublinhado|`a { text-decoration: underline; }`|
|Tirar sublinhado de links|`a { text-decoration: none; }`|
|Riscado (ex: preço promocional)|`span { text-decoration: line-through; }`|

---

## ⚠️ Dicas importantes

1. A decoração **afeta apenas o texto**, não as bordas ou a caixa.
    
2. Algumas decorações **vêm por padrão**, como `underline` em links `<a>`.
    
3. Se quiser remover a decoração de um link:
    
    ```css
    a {
      text-decoration: none;
    }
    ```
    

---

Se quiser, posso te dar alguns exercícios práticos com `text-decoration`. Deseja isso?