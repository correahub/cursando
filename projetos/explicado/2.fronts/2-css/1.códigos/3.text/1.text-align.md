### 📘 O que é `text-align`?

A propriedade `text-align` do CSS **define o alinhamento horizontal do texto** (ou de outros conteúdos em linha) dentro de um elemento.

---

### 🧠 Como ela funciona?

Quando você aplica `text-align` a um **elemento pai** (como uma `<div>`, por exemplo), você está dizendo:

> “Alinhe o texto (ou conteúdo em linha) que estiver dentro de mim da seguinte maneira...”

---

### 🛠️ Os valores mais usados:

|Valor|O que faz|
|---|---|
|`left`|Alinha o texto à **esquerda** (padrão na maioria dos navegadores)|
|`right`|Alinha o texto à **direita**|
|`center`|**Centraliza** o texto|
|`justify`|Alinha o texto dos dois lados (esquerda e direita), ajustando os espaços|

---

### ✅ Exemplo prático (com explicação):

Imagine que você está criando um parágrafo de texto e quer centralizar o conteúdo. Aqui vai um exemplo:

```html
<!DOCTYPE html>
<html>
<head>
  <style>
    .centralizado {
      text-align: center;
    }
  </style>
</head>
<body>

  <div class="centralizado">
    <p>Bem-vindo ao meu site! Espero que você goste do conteúdo.</p>
  </div>

</body>
</html>
```

#### 💡 O que está acontecendo aqui?

- Criamos uma `div` com a **classe** `centralizado`.
    
- No CSS, usamos `text-align: center;` para **centralizar o texto** que está dentro dessa `div`.
    
- O parágrafo `<p>` herda esse alinhamento e aparece **centralizado na tela**.
    

---

### 📝 Dica para iniciantes:

- O `text-align` **não move a `div` inteira**, apenas o **conteúdo de texto (ou elementos em linha)** dentro dela.
    
- Se quiser centralizar uma `div`, aí é outro assunto — usamos outras propriedades como `margin`.
    
