
---

### 🧱 **1. `@font-face { ... }`**

A regra **`@font-face`** serve para **importar e registrar uma fonte personalizada** no CSS, permitindo usá-la mesmo que **não esteja instalada no computador do usuário**.

---

#### 📌 Estrutura:

```css
@font-face {
  font-family: nome-da-fonte;
  src: url(caminho/para/fonte.formato) format("tipo");
}
```

- **`font-family`** → define o **nome da fonte** (apelido) que você vai usar no CSS.
    
- **`src`** → indica o **caminho do arquivo da fonte** e o **formato** (truetype, woff, woff2, svg, etc.).
    

---

### 💡 **Explicando seu exemplo:**

```css
@charset "UTF-8";

@font-face {
  font-family: 'Nadia-serif';
  src: local('Nadia-serif'), local('Times New Roman'),
       url('/fonts/nadia-serif.woff2') format('woff2'),
       url('/fonts/nadia-serif.woff') format('woff');
  font-weight: normal;
  font-style: normal;
}
```

🔹 **`font-family: sword;`**  
Define o nome da fonte personalizada. Você poderá usá-la assim:

```css
h1 {
  font-family: sword, serif;
}
```

🔹 **`src:`**  
Lista os **arquivos da fonte em vários formatos**, garantindo **compatibilidade entre navegadores**:

- `.ttf` → formato TrueType (antigo, ainda compatível).
    
- `.woff` e `.woff2` → formatos otimizados para web (mais leves e modernos).
    
- `.eot` e `.svg` → usados por navegadores antigos (como Internet Explorer e versões antigas de iOS).
    

O navegador escolhe **automaticamente o formato que ele suporta melhor**.

---

### ✅ **Resumo:**

| Regra               | Função                                             |
| ------------------- | -------------------------------------------------- |
| `@charset "UTF-8";` | Define a codificação do arquivo CSS                |
| `@font-face`        | Importa fontes personalizadas                      |
| `font-family`       | Dá um nome para a fonte                            |
| `src`               | Indica o caminho e o formato dos arquivos da fonte |
| local()             | procura fontes no PC do usuário, antes de baixar.  |

---

💡 **Dica:** sempre use formatos modernos (`woff` e `woff2`) — são leves, rápidos e amplamente suportados.