# :read-write` é uma pseudo-class que aplica estilos aos elementos que podem ser editados pelo usuário, permitindo digitação.

---

 ✏️ O que é `:read-write`?

Imagine que um campo de formulário (como um input ou textarea) é um **caderninho** 📒.

- Às vezes você **pode escrever** nesse caderninho.
    
- Às vezes ele está **travado** e você só pode ler.
    

O `:read-write` é usado para selecionar **todos os campos que podem ser escritos** — ou seja, os campos que **não estão desativados, nem bloqueados**, e que aceitam texto.

É como dizer:

> _"Me mostre todos os cadernos onde eu posso escrever!"_ ✏️😄

---

# 🎨 Exemplo simples

```css
input:read-write {
  background-color: lightyellow;
}
```

**O que acontece?**  
Todos os `<input>` que **aceitam digitação** ficam com o fundo amarelinho.

Se o input estiver com atributos como:

- `disabled`
    
- `readonly`
    

…ele **não** será selecionado por `:read-write`.

---

# 💡 Quando um elemento é “read-write”?

Ele é **read-write** quando:

- aceitar digitação ✏️
    
- não estiver `disabled`
    
- não estiver `readonly`
    
- tiver um tipo válido (texto, número, email, etc.)
    

