
**`:first-child` é uma pseudo-class que aplica estilos no primeiro filho de um elemento.**  
**`:last-child` é uma pseudo-class que aplica estilos no último filho de um elemento.** 

---

# 🍭 O que é `:first-child`?

Imagine que você tem uma **caixa cheia de filhos** (elementos) dentro dela.  
O **primeiro filho** que está lá dentro é o que recebe o `:first-child`.

É como uma fila de crianças:

👦👧👧👧  
↑  
Esse é o **primeiro**.

O `:first-child` estiliza **somente** esse primeiro.

---

## 🎨 Exemplo

```css
div p:first-child {
  color: red;
}
```

**O que acontece?**  
Apenas o **primeiro** `<p>` dentro da `<div>` fica vermelho.

---

# 🍬 O que é `:last-child`?

Agora imagine a mesma fila de crianças, mas você olha para o **último** da fila:

👦👧👧👧  
↑  
Esse é o **último**.

O `:last-child` estiliza **somente** o último filho do elemento.

---

## 🎨 Exemplo

```css
div p:last-child {
  color: blue;
}
```

**O que acontece?**  
Apenas o **último** `<p>` dentro da `<div>` fica azul.

---

