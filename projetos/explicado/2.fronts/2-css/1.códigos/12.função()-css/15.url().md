**`url()`** é uma função do CSS usada para **inserir links de arquivos externos, como imagens, fontes ou outros recursos**.

Exemplo:

```css
div {
  background-image: url("imagem.jpg");
}
```

Aqui, a imagem `"imagem.jpg"` será usada como **plano de fundo** do elemento.