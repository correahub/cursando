# **`color-mix()`** é uma função do CSS usada para **misturar duas cores em uma proporção definida**.

Exemplo:

```css
div {
  background-color: color-mix(in srgb, red 50%, blue 50%);
}
```

Aqui, o fundo do `div` será **uma mistura igual de vermelho e azul**, resultando em roxo.