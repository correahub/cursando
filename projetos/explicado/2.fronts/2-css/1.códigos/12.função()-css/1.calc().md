# calc() é uma função do CSS usada para **realizar cálculos matemáticos dentro de valores de atributos**, como somar, subtrair, multiplicar ou dividir unidades.

Exemplo:

```css
div {
  width: calc(100% - 50px);
  height: calc(50px + 2em);
}
```
