# transition é o shorhand para  property, duration, timing-fanctions, delay.

---

# 🧒 Explicação bem simples

Imagine que você tem quatro peças diferentes para fazer uma animação:

1. **transition-property** → o que vai mudar 🧩
    
2. **transition-duration** → quanto tempo dura ⏱️
    
3. **transition-timing-function** → o ritmo da mudança 🎢
    
4. **transition-delay** → quanto tempo espera antes de começar ⏳
    

A propriedade **transition:** é como **juntar tudo isso em uma única frase**.

É como dizer:

> “Muda _esta coisa_, durante _tanto tempo_, com _tal ritmo_, e começando depois de _tal atraso_.”

Tudo em **uma única linha**.

---

# 🎪 Exemplo completo

```css
div {
  transition: width 1s ease-in-out 0.5s;
}
```

Isso significa:

- **width** → propriedade que vai mudar
    
- **1s** → duração da transição
    
- **ease-in-out** → ritmo da transição
    
- **0.5s** → atraso antes de começar
    

É a mesma coisa que:

```css
transition-property: width;
transition-duration: 1s;
transition-timing-function: ease-in-out;
transition-delay: 0.5s;
```

Só que muito mais organizado! 😄

---

# ✨ Você também pode simplificar

Se você quiser só **propriedade + duração**, pode:

```css
transition: width 1s;
```

O navegador vai preencher o resto com valores padrões:

- timing-function → `ease`
    
- delay → `0s`
    

---

# 🎯 Você pode animar _tudo de uma vez_

Se quiser animar qualquer propriedade que puder mudar:

```css
transition: all 0.5s ease;
```

`all` significa:

> “Anime qualquer propriedade que mudar.”


transition é a forma abreviada que combina propriedade, duração, ritmo e atraso de uma transição CSS em uma única linha.