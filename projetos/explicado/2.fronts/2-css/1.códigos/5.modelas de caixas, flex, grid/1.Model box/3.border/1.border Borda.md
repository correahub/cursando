
---

## 🧱 O que é a propriedade `border` em CSS?

A propriedade `border` é usada para **criar uma borda ao redor de um elemento HTML**, como um parágrafo, uma imagem, uma caixa de texto, etc.

Pense nela como a **moldura de um quadro** — ela contorna o conteúdo.

---

## 🎯 Para que serve?

- Para **destacar** áreas da página.
    
- Para **separar** visualmente partes do conteúdo.
    
- Para **estilizar** elementos (com cor, espessura, estilo).
    
- Para ajudar a **organizar** o layout de uma página.
    

---

## ⚙️ Como usar?

A propriedade `border` pode ser usada de várias formas, mas a forma mais simples é:

```css
	  border-top: 30px;   /* Espaço no topo */
      border-right: 50px; /* Espaço na direita */
      border-bottom: 20px;/* Espaço embaixo */
      border-left: 10px;
```

### Exemplos de valores:

|Parte|Exemplo|Significado|
|---|---|---|
|Espessura|`1px`, `5px`|Define a **largura** da borda|
|Estilo|`solid`|Define o **tipo de linha** da borda|
|Cor|`black`, `red`|Define a **cor** da borda|

---

## 🧪 Exemplo prático

Vamos criar uma **caixa com texto e uma borda ao redor**, tudo com HTML e CSS.

### 🧾 HTML (estrutura da página):

```html
<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <title>Exemplo de Borda</title>
  <style>
    .caixa-com-borda {
      border-top: 30px;   /* Espaço no topo */
      border-right: 50px; /* Espaço na direita */
      border-bottom: 20px;/* Espaço embaixo */
      border-left: 10px;          /* Largura da caixa */
      text-align: center;     /* Alinha o texto ao centro */
    }
  </style>
</head>
<body>

  <div class="caixa-com-borda">
    Olá! Eu tenho uma borda azul ao meu redor.
  </div>

</body>
</html>
```

---

## 🧠 Explicando o código passo a passo:

1. **HTML** define uma `div` (uma caixa genérica).
    
2. Essa `div` tem a **classe** `caixa-com-borda`, que usamos para aplicar estilos no CSS.
    
3. No **CSS**:
    
    - `border: 3px solid blue;` cria uma borda azul sólida com 3 pixels de espessura.
        
    - `padding: 20px;` adiciona espaço entre o texto e a borda.
        
    - `width: 300px;` define a largura da caixa.
        
    - `text-align: center;` centraliza o texto.
        

---

## 📌 Dica para praticar:

Você pode copiar esse exemplo em um **arquivo chamado `index.html`** no seu computador, abrir com o navegador e ver o resultado.

---

Se quiser, posso te mostrar também como fazer bordas arredondadas, bordas só de um lado, bordas com cores diferentes nos lados, etc. É só pedir!