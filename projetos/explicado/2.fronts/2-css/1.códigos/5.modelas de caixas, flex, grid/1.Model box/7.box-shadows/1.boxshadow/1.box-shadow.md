
---

## 🧠 O que é `box-shadow`?

> `box-shadow` é uma **propriedade CSS** que adiciona uma **sombra ao redor de uma caixa**, como uma `<div>`, botão, imagem, etc.

É como se colocássemos uma **luz invisível** e a caixa jogasse uma **sombra** atrás dela.

---

### 🎨 Para que serve?

- Dá sensação de profundidade.
    
- Deixa elementos mais bonitos, como se estivessem “flutuando”.
    
- É muito usado em **botões modernos**, **cartões**, **janelas flutuantes**, etc.
    

---

## ✅ Exemplo prático simples

```html
<!DOCTYPE html>
<html>
<head>
  <style>
    .caixa-com-sombra {
      width: 200px;
      height: 100px;
      background-color: lightblue;
      box-shadow: 5px 5px 10px gray;
    }
  </style>
</head>
<body>

  <h2>Caixa com sombra</h2>
  <div class="caixa-com-sombra"></div>

</body>
</html>
```

---

## 🔍 Explicando o `box-shadow`

```css
box-shadow: 5px 5px 10px gray;
```

Esses valores têm significados:

```
box-shadow: deslocamento-horizontal deslocamento-vertical borrão cor;
```

- `5px` → a sombra vai 5 pixels para **a direita**.
    
- `5px` → a sombra vai 5 pixels para **baixo**.
    
- `10px` → o **borrão** da sombra (quanto maior, mais suave).
    
- `gray` → a **cor** da sombra.
    

---

### 🧱 Quer ver a diferença?

#### Sem sombra:

```css
box-shadow: none;
```

#### Sombra leve e suave:

```css
box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.2);
```

#### Sombra forte e escura:

```css
box-shadow: 10px 10px 20px rgba(0, 0, 0, 0.6);
```

#### Sombra interna (dentro da caixa):

```css
box-shadow: inset 0px 0px 10px rgba(0, 0, 0, 0.3);
```

---

## 📐 Exemplo com botão moderno

```html
<button style="
  padding: 10px 20px;
  background-color: #4CAF50;
  color: white;
  border: none;
  border-radius: 8px;
  box-shadow: 0 4px 8px rgba(0,0,0,0.3);
">
  Clique aqui
</button>
```

Esse botão:

- Tem bordas arredondadas (`border-radius`)
    
- Uma sombra suave que dá destaque e profundidade
    

---

## 📝 Resumo

|Valor|Explicação|
|---|---|
|`box-shadow`|Adiciona uma sombra à caixa|
|`5px 5px 10px gray`|5px à direita, 5px abaixo, 10px de borrão, cor cinza|
|`inset`|Faz a sombra aparecer **dentro** da caixa|
|`rgba()`|Permite sombra com transparência|

---

## 💡 Dica

Você pode testar sombras em tempo real aqui:  
🔗 [https://cssmash.com/css-box-shadow-generator/](https://cssmash.com/css-box-shadow-generator/)  
É um **gerador visual**: arraste e copie o código!

---

Se quiser, posso te propor um **exercício prático** para criar um "cartão bonito com sombra e bordas arredondadas", tipo os usados em sites modernos. Quer fazer?