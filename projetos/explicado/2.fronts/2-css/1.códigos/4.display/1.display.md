
---

## 🧠 O que é a propriedade `display` em CSS?

A propriedade `display` define **como um elemento HTML será exibido na página**.  
Ela é **uma das propriedades mais importantes do CSS** porque **controla o comportamento básico de layout** dos elementos.

---

## 🧱 Pense assim:

Imagine que os elementos HTML são **caixinhas**. A propriedade `display` decide **como essas caixinhas se organizam**:

- Vão ficar uma do lado da outra?
    
- Uma embaixo da outra?
    
- Vão se esconder?
    
- Vão agir como blocos grandes ou como pedacinhos de texto?
    

---

## 🎯 Para que serve?

Serve para dizer ao navegador:

> "Ei, eu quero que esse elemento se comporte assim!"

Você usa `display` para criar layouts, controlar como os itens aparecem, alinham-se e se posicionam na tela.

---

## 🎓 Os tipos mais comuns de `display`

|Valor|O que faz|
|---|---|
|`block`|O elemento ocupa **toda a largura disponível**. Vai sempre para uma **nova linha**. (Ex: `<div>`)|
|`inline`|O elemento **fica na mesma linha** que outros, só ocupa o espaço necessário. (Ex: `<span>`)|
|`inline-block`|Parecido com `inline`, **mas permite definir largura e altura**.|
|`none`|**Esconde completamente** o elemento da página.|
|`flex`|Ativa o **flexbox**, um sistema moderno de layout responsivo.|
|`grid`|Ativa o **CSS grid**, um sistema de layout em linhas e colunas.|

---

## 🧪 Exemplo prático para um iniciante

Vamos fazer um exemplo simples com `block`, `inline` e `none`.

### 📝 Código HTML + CSS:

```html
<!DOCTYPE html>
<html>
<head>
  <style>
    .caixa1 {
      background-color: lightblue;
      display: block;
      padding: 10px;
      margin-bottom: 10px;
    }

    .caixa2 {
      background-color: lightgreen;
      display: inline;
      padding: 10px;
    }

    .caixa3 {
      background-color: pink;
      display: none;
    }
  </style>
</head>
<body>

  <div class="caixa1">Eu sou display: block</div>
  <span class="caixa2">Eu sou display: inline</span>
  <div class="caixa3">Você não vai me ver (display: none)</div>

</body>
</html>
```

---

## 🔍 O que vai acontecer?

- A **caixa1** vai aparecer como uma **barra azul** que ocupa toda a largura (porque `display: block`).
    
- A **caixa2** vai aparecer **na mesma linha** que qualquer outro texto (porque `display: inline`).
    
- A **caixa3** **não vai aparecer**, está escondida (`display: none`).
    

---

## ✅ Como testar?

1. Copie o código acima.
    
2. Cole em um editor de HTML como o [JSFiddle](https://jsfiddle.net/), [CodePen](https://codepen.io/), ou no **Bloco de Notas**.
    
3. Salve como `exemplo.html`.
    
4. Abra no seu navegador.
    

---

## 🧩 Dica para aprender mais

Depois de entender `block`, `inline` e `none`, o próximo passo é estudar `display: flex` e `grid` para criar **layouts mais avançados**, como colocar elementos lado a lado, centralizar na tela etc.

Se quiser, posso te mostrar um exemplo com `display: flex` também.

Quer ver?