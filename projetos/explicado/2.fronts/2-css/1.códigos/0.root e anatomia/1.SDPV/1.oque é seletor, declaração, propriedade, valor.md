folhas de cascata em estilos

---
```
* {  
   height: 100%;

       } 
       
       
       CONFIGURAÇÃO GLOBAL DAS CSS
```
### 1. **Seletor**:

É **quem você quer vestir**.

Imagine que você tem várias bonecas e diz:

> “Essa aqui com vestido azul é a que eu quero vestir agora.”

No CSS, você escolhe quem vai receber o estilo. Pode ser um título, um parágrafo, um botão…

👉 **Exemplo de seletor:**

```
p
```

Isso quer dizer: **“Quero vestir (estilizar) todos os parágrafos da página.”**

---

### 2. **Declaração**:

É **o que você quer fazer com esse seletor**.

Você está dizendo:

> “Quero que o parágrafo fique vermelho” ou “Quero que o botão fique grande”.

Cada declaração **tem duas partes**: a **propriedade** e o **valor**.

👉 Um conjunto de declarações fica dentro de chaves `{}`.

---

### 3. **Propriedade**:

É **o que você quer mudar** na roupa.

Pode ser a **cor**, o **tamanho da letra**, o **espaço**, o **fundo**…

👉 Exemplo de propriedade:

```
`color`
```

Isso quer dizer que você quer mudar a **cor do texto**.

---

### 4. **Valor**:

É **como você quer que fique** essa propriedade.

👉 Exemplo de valor:

```
`red`
```

Isso quer dizer **“quero que fique vermelho”**.

---

### Juntando tudo:

Vamos montar uma regra completa:

```
p {
  color: red;
}
```

- **`p`** → é o **seletor** (todos os parágrafos)
    
- **`color`** → é a **propriedade** (cor do texto)
    
- **`red`** → é o **valor** (vermelho)
    
- **`color: red;`** → é uma **declaração**
    
- Tudo dentro das `{ }` é o conjunto de **declarações** aplicadas ao seletor
    

---

### Outro exemplo, como se fosse uma receita para um botão:


```
button {
  background-color: blue;
  font-size: 20px;
}
```

- Seletor: `button` (todos os botões)
    
- Declaração 1: `background-color: blue;` → o fundo será azul
    
- Declaração 2: `font-size: 20px;` → o texto do botão será grandão
    

---

### Resumo fácil:

|Parte|Explicação simples|Exemplo|
|---|---|---|
|Seletor|Quem vai receber o estilo|`p` ou `button`|
|Propriedade|O que você quer mudar|`color`|
|Valor|Como você quer que fique|`red`|
|Declaração|Propriedade + valor|`color: red;`|
|Regra CSS|Seletor + chaves + declarações|`p { color: red; }`|