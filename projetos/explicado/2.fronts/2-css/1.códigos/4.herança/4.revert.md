
# revert redefine a propriedade para o valor vindo da cascata anterior, como se a regra atual não existisse.






















---

## 📌 O que é `revert`?

**`revert` significa:  
“Volta para o estilo que existia antes de eu mexer.”** ⏪

👉 Ele **desfaz regras do autor (seu CSS)** e retorna para o **estilo anterior da cascata**.

---

## 🧠 Ideia bem simples

Imagine camadas 🧱:

1. Estilo do navegador 🌐 (user-agent)
    
2. Estilo do usuário 👤 (se existir)
    
3. Seu CSS ✍️
    

👉 `revert` apaga **só o que você escreveu** e volta para a camada de baixo.

---

## 🧠 Definição técnica (bem simples)

**`revert` redefine a propriedade para o valor vindo da cascata anterior, como se a regra atual não existisse.** 

---

## ✍️ Exemplo prático

```css
button {
  background: blue;
  color: white;
}

button.reset {
  background: revert;
  color: revert;
}
```

🔹 O que acontece?

- O botão normal é azul
    
- O botão `.reset` volta ao **estilo padrão do navegador**  
    (cinza, borda padrão, etc.)
    

---

## 📎 Diferença importante

👉 **`initial`** volta para o valor técnico do CSS  
👉 **`revert`** volta para o valor **real que estava sendo usado antes**

Exemplo:

```css
button {
  border: none;
}

button {
  border: revert;
}
```

🔹 Resultado:

- A borda padrão do navegador **volta a aparecer**
    

Com `initial`, a borda continuaria `none`.

---

## 🧪 Comparação mental rápida

|Valor|O que faz|
|---|---|
|`inherit`|Copia do pai 👨‍👦|
|`initial`|Valor padrão da spec 🔄|
|`unset`|Decide entre herdar ou resetar 🤔|
|`revert`|Volta para o estilo anterior ⏪|

---

## 🧩 Quando usar `revert`?

Use quando você quer:

- Recuperar estilos nativos do navegador
    
- “Cancelar” um reset.css
    
- Remover regras globais sem quebrar o padrão
    

---

## 🧠 Frase para memorizar

👉 **`revert` = finge que essa regra nunca existiu** ⏪✨

---

## 🧾 Definição curta (exata, como você pediu)

**`revert` é um valor que restaura a propriedade para o valor da cascata anterior, ignorando o CSS atual.** 
