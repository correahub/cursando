
---

## 🧠 O que é a propriedade `color` no CSS?

A propriedade `color` no CSS serve para **definir a cor do texto** de um elemento HTML.

Ela **não** muda a cor do fundo (para isso usamos `background-color`), apenas do **texto**.

---

## 🛠️ Como usar a propriedade `color`?

Você escreve `color: valor;`, onde **"valor"** pode ser:

- Um **nome de cor**: `red`, `blue`, `green`, etc.
    
- Um **código hexadecimal**: `#FF0000` (vermelho), `#000000` (preto), etc.
    
- Um **RGB**: `rgb(255, 0, 0)` (vermelho)
    
- Ou **HSL**, mas vamos focar no básico por enquanto.
    

---

## 📌 Exemplo prático

Imagine que você quer escrever um parágrafo com texto azul.

### Código HTML + CSS:

```html
<!DOCTYPE html>
<html>
<head>
  <style>
    p {
      color: blue;
    }
  </style>
</head>
<body>

  <p>Olá, eu sou um texto azul!</p>

</body>
</html>
```

### O que está acontecendo aqui?

1. Temos um parágrafo (`<p>`).
    
2. No `<style>`, usamos `p { color: blue; }` — ou seja:
    
    - “Todos os parágrafos terão o texto azul.”
        

---

## ✅ Dica extra

Você pode testar isso copiando esse código e colando em um arquivo `.html`, depois abrindo ele no navegador!

---
