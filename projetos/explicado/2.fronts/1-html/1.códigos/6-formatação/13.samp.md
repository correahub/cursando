# `<samp>` é um elemento de marcação usado para marcar  a saída ou resposta de um programa, sistema ou computador.


























---

## 🔤 O que é `<samp>`?

Pense assim 👇  
O **`<samp>`** é uma **etiqueta do HTML usada para mostrar a resposta de um computador** 🤖💬

Ou seja:  
👉 **é o texto que o sistema, programa ou terminal “fala” para o usuário**.

---

## 🖥️ Um exemplo simples

Imagine um computador dizendo algo:

```html
<p>O sistema respondeu: <samp>Arquivo não encontrado</samp></p>
```

📌 Aqui:

- O texto **“Arquivo não encontrado”** é algo que o **computador mostrou**
    
- Por isso usamos `<samp>`
    

---

## 🎮 Pense como um jogo

- 👦 Usuário digita um comando
    
- 🖥️ Computador responde algo
    

Exemplo:

```html
<p>Saída do programa: <samp>Level up! Você agora é nível 5.</samp></p>
```

👉 Essa frase é a **resposta do sistema**, então `<samp>` é perfeito 😄

---

## 🧠 O significado semântico (o mais importante)

O navegador entende que:

> “Isso aqui não é um texto normal.  
> É uma **mensagem gerada por um sistema**.”

Isso ajuda:

- leitores de tela ♿
    
- SEO 🔍
    
- organização correta do HTML 🧱
    

---

## 🎨 Aparência padrão

Por padrão, o `<samp>`:

- usa **fonte monoespaçada** (tipo terminal)
    
- parece com `<code>`
    

⚠️ **Mas cuidado:**  
Eles **não são a mesma coisa**!

---

## 🆚 Diferença entre `<samp>`, `<code>` e `<kbd>`

Vamos comparar bem fácil 👇

|Tag|Serve para quê?|
|---|---|
|`<kbd>`|⌨️ O que o **usuário digita**|
|`<code>`|🧩 Código em si|
|`<samp>`|🖥️ O que o **computador responde**|

### Exemplo completo:

```html
<p>
  Digite <kbd>ls</kbd> e o sistema mostrará
  <samp>Documentos Downloads Imagens</samp>
</p>
```

✨ Isso é **HTML semântico perfeito** ✨

---

## ❌ Quando NÃO usar `<samp>`

Não use `<samp>` para:

- texto comum ❌
    
- explicações ❌
    
- nomes de variáveis ❌
    

Use **somente quando for saída de sistema** 🖥️

---
