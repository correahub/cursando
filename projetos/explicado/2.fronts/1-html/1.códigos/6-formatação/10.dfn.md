# `<dfn> é um elemento usada para marcar um termo que está sendo definido pela primeira vez no conteúdo.

Uma vez por definição.






















---

## 🧠 O que é `<dfn>` no HTML?

Pense assim 👶:

👉 O `<dfn>` é uma **etiqueta** que você coloca em uma palavra **quando ela está sendo explicada pela primeira vez**.

É como dizer:

> “Ei! Essa palavra aqui é importante, presta atenção porque agora eu vou explicar o que ela significa!” 📌

---

## 📘 Exemplo bem simples

```html
<p>
  <dfn>HTML</dfn> é a linguagem usada para criar páginas da web.
</p>
```

Aqui acontece isso 👇

- **HTML** está dentro do `<dfn>`
    
- Isso diz ao navegador e aos leitores:
    
    > “HTML está sendo definido agora” 📖
    

---

## 🤖 Para que isso serve?

### 1️⃣ Para **semântica** (significado)

O navegador entende melhor o conteúdo 🧠

### 2️⃣ Para **acessibilidade**

Leitores de tela avisam que aquilo é uma definição 🔊

### 3️⃣ Para **SEO**

Motores de busca entendem melhor termos importantes 🔍

---

## 🎨 O `<dfn>` muda o visual?

👉 **Por padrão**, ele geralmente deixa o texto em _itálico_  
Mas isso **não é regra**, é só o estilo padrão do navegador.

Você pode mudar com CSS 🎨:

```css
dfn {
  font-style: normal;
  font-weight: bold;
  color: blue;
}
```

---

## ⚠️ Regra importante

❌ Não use `<dfn>` toda vez que a palavra aparecer  
✅ Use **somente na primeira vez**, quando estiver explicando

---

## ✅ Forma correta e semântica (com `title`)

```html
<p>
  <dfn title="HyperText Markup Language">HTML</dfn> é usado para estruturar páginas.
</p>
```

Assim:

- Humanos entendem 👨‍👩‍👧
    
- Máquinas entendem 🤖
    
- Leitores de tela entendem 🔊
    

---

## 📌 Definição técnica (bem direta)

**`<dfn>` é uma tag HTML semântica usada para marcar um termo que está sendo definido pela primeira vez no conteúdo.** 🏷️📘✨

