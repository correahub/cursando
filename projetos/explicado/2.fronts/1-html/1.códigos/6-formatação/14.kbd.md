# O **`<kbd>`** é um elemento usada para marcar **teclas do teclado** ou **comandos que o usuário deve digitar**
























---

## 🔤 O que é `<kbd>` no HTML?

O **`<kbd>`** é uma _tag HTML_ usada para mostrar **teclas do teclado** ou **comandos que o usuário deve digitar** ⌨️

Pense assim:  
👉 Sempre que você quiser dizer **“aperte essa tecla”** ou **“digite isso”**, você usa `<kbd>`.

---

## 🧠 Para que ele serve?

Ele serve para:

- Mostrar **teclas do teclado**
    
- Mostrar **atalhos**
    
- Mostrar **comandos digitados**
    

Exemplos do dia a dia:

- `Ctrl`
    
- `Alt`
    
- `Delete`
    
- `Ctrl + C`
    
- `Ctrl + V`
    
- `npm install`
    

---

## ✍️ Exemplo simples

```html
<p>Pressione <kbd>Enter</kbd> para continuar.</p>
```

👉 Isso quer dizer claramente:  
**“Aperte a tecla Enter”**

---

## 🔗 Exemplo com atalhos

```html
<p>Use <kbd>Ctrl</kbd> + <kbd>C</kbd> para copiar.</p>
```

👉 Aqui você está ensinando um **atalho de teclado** 🎯

---

## 🎨 Aparência padrão do `<kbd>`

Por padrão, o navegador mostra o `<kbd>` assim:

- Fonte diferente (parece teclado)
    
- Um fundo cinza
    
- Bordinha leve
    

Isso ajuda o leitor a **bater o olho e entender que é uma tecla** 👀✨

---

## 🎯 Quando usar `<kbd>`?

Use `<kbd>` quando:

- O usuário **precisa apertar uma tecla**
    
- O usuário **precisa digitar algo**
    
- Você está explicando **atalhos ou comandos**
    

❌ Não use para:

- Definições → use `<dfn>`
    
- Abreviações → use `<abbr>`
    
- Código → use `<code>`
    

Cada tag tem sua função certinha 🧩

---

## 📌 Definição técnica (bem simples)

**`<kbd>`** é uma _tag HTML semântica_ usada para representar **entrada do usuário via teclado ou comandos digitados**, melhorando a clareza e a acessibilidade do conteúdo. ⌨️✨

---

Se quiser, posso:

- Comparar `<kbd>` vs `<code>`
    
- Mostrar como **estilizar `<kbd>` com CSS**
    
- Explicar quando usar `<kbd>` em documentação técnica 📘
    

É só falar 😄