### 📌 O que é a tag `<code>`?

A tag `<code>` serve para **representar trechos de código** de programação. Ela **não preserva espaços, tabulações ou quebras de linha** automaticamente, e seu principal objetivo é semântico: indicar ao navegador e a ferramentas de leitura que aquilo é código.

---

### 📌 O que é a tag `<pre>`?

A tag `<pre>` vem de "pré-formatado" (preformatted) e serve para **preservar exatamente os espaços, tabulações e quebras de linha** como estão no HTML. É muito usada junto com a `<code>` para mostrar blocos de código com a formatação correta.

---

### ✅ Exemplo prático e combinado:

Imagine que você quer mostrar este código em uma página:

n = 5
if n == 5
print('olá,mundo!')

Se você escrever apenas com `<code>`:

<code>n = 5 
if n == 5
print('olá,mundo!')</code>

➡️ O navegador **vai ignorar os espaços e quebras de linha**, então vai ficar tudo em **uma linha só**, mesmo que você tenha escrito em várias.

Agora veja com `<pre>` + `<code>`:
<pre>
	<code> 
		n = 5 
		if n == 5
		print('olá,mundo!') 
	</code>
</pre>

➡️ Agora sim! O navegador **mantém a indentação e as quebras de linha**, exatamente como você escreveu.

---

### 🎓 Resumo:

| Tag           | Função principal                                      | Preserva formatação? | Ideal para...              |
| ------------- | ----------------------------------------------------- | -------------------- | -------------------------- |
| `<code>`      | Mostrar código em linha ou trechos curtos             | ❌ Não                | Trechos pequenos de código |
| `<pre>`       | Mostrar texto com formatação exata (espaços, quebras) | ✅ Sim                | Blocos inteiros de código  |
| `<pre><code>` | Combinação ideal para mostrar códigos formatados      | ✅ Sim                | Blocos visuais de código   |

----

## 💡 1️⃣ O que é a tag `<samp>`?

A tag `<samp>` vem da palavra **sample**, que em inglês significa **exemplo**.  
Mas, no HTML, ela tem um sentido mais específico:

> Ela serve para **mostrar o resultado (ou saída)** que um **programa, comando ou sistema de computador** exibiria.

👩‍💻 Em outras palavras:

- Você usa `<code>` para **mostrar o código que escreve**.
    
- E usa `<samp>` para **mostrar o que o computador responde**.
    

---

## 🧠 2️⃣ — Exemplo simples

Imagine que você está ensinando um comando no terminal:

```html
<p>Digite o comando:</p>
<code>echo Olá, mundo!</code>

<p>Saída esperada:</p>
<samp>Olá, mundo!</samp>
```

🗣️ Isso quer dizer:

- `<code>` → mostra o **comando que o usuário digita**.
    
- `<samp>` → mostra o **resultado que o computador exibe**.
    

E no navegador, normalmente o texto dentro de `<samp>` aparece em uma **fonte monoespaçada** (tipo de máquina de escrever), igual à do `<code>`.

---

## ⚙️ 3️⃣ — Como ela funciona na prática

O `<samp>` não muda o significado visual por si só, mas muda o **sentido semântico** do texto.

Ou seja:

- O navegador entende que aquilo é **uma saída gerada automaticamente**.
    
- Leitores de tela e buscadores entendem que é **texto retornado por um sistema**.
    

Isso ajuda muito quando você está **documentando código, tutoriais ou manuais técnicos**.

---

## 🎨 4️⃣ — Personalizando com CSS

Você pode deixar a saída mais bonitinha, como se fosse de um terminal:

```html
<p>Saída:</p>
<samp>Arquivo salvo com sucesso!</samp>
```

```css
samp {
  background-color: #2d2d2d;
  color: #00ff88;
  padding: 0.4em 0.8em;
  border-radius: 6px;
  font-family: monospace;
  display: inline-block;
}
```

🧩 Resultado visual:

```
Arquivo salvo com sucesso!
```

(com aquele estilo de “tela de terminal”)

---

## 🔍 5️⃣ — Diferença entre `<samp>`, `<code>`, `<kbd>` e `<var>`

Essas quatro tags fazem parte da **família de tags de código** do HTML, mas cada uma tem uma função específica:

|Tag|Significado|Exemplo|Uso|
|---|---|---|---|
|`<code>`|Mostra o código-fonte que o programador escreve|`<code>print("Olá")</code>`|Código de exemplo|
|`<kbd>`|Mostra algo que o usuário **digita** no teclado|`<kbd>Ctrl</kbd> + <kbd>S</kbd>`|Entrada do usuário|
|`<var>`|Mostra uma **variável** ou símbolo matemático|`<var>x</var> + <var>y</var>`|Representar variáveis|
|`<samp>`|Mostra a **saída** que o computador devolve|`<samp>Arquivo salvo!</samp>`|Resultado do programa|

Então:

- `<kbd>` → o que o usuário **digita**
    
- `<code>` → o que o **programador escreve**
    
- `<samp>` → o que o **computador responde**
    

---

## 🪄 6️⃣ — Exemplo completo e semântico

```html
<p>O usuário digita:</p>
<kbd>python</kbd> <kbd>main.py</kbd>

<p>O código executa:</p>
<code>print("Olá, Lorty!")</code>

<p>E a saída é:</p>
<samp>Olá, Lorty!</samp>
```

➡️ HTML entende cada parte direitinho:

- `<kbd>` → entrada do usuário.
    
- `<code>` → código executado.
    
- `<samp>` → saída do programa.
    

---

## ✅ Resumo rápido:

|Tag|Função|Exemplo|Mostra o quê|
|---|---|---|---|
|`<samp>`|Exibe o **resultado** de um programa|`<samp>Olá, mundo!</samp>`|Saída gerada pelo computador|

---

Excelente escolha, Lorty 😎  
A tag `<kbd>` é uma daquelas pequenas, mas superimportantes para quem gosta de **ensinar, documentar comandos** ou **mostrar atalhos de teclado** em HTML.  
Vamos entender tudo sobre ela de um jeito bem claro — como se estivéssemos construindo um tutorial de computador juntos 🖥️⌨️

---

## 💡 1️⃣ — O que é a tag `<kbd>`?

A tag `<kbd>` vem da palavra inglesa **“keyboard”** (teclado).  
Ela serve para **representar algo que o usuário deve digitar** no teclado.

👉 Em resumo:

> **`<kbd>` mostra uma entrada de teclado, comando ou tecla que o usuário precisa pressionar.**

---

### 🧩 Exemplo simples:

```html
<p>Para salvar o arquivo, pressione <kbd>Ctrl</kbd> + <kbd>S</kbd>.</p>
```

🗣️ Isso quer dizer:

> “O usuário precisa apertar as teclas **Ctrl** e **S** para salvar.”

O navegador entende que o conteúdo dentro de `<kbd>` é **entrada do usuário** (algo digitado ou pressionado).

---

## 🧠 2️⃣ — Para que serve (além de atalhos)?

Você pode usar `<kbd>` para **qualquer entrada digitada**:

- Teclas (Ctrl, Alt, Enter, etc.);
    
- Palavras ou comandos que o usuário digita (como no terminal);
    
- Combinações de teclas (Ctrl + Shift + Esc);
    
- Sequências de texto que o usuário deve escrever.
    

### Exemplo com comandos:

```html
<p>Digite <kbd>npm install</kbd> no terminal para instalar as dependências.</p>
```

🧩 O `<kbd>` aqui mostra que **é algo que o usuário deve digitar**, não o resultado do programa.

---

## 💻 3️⃣ — Diferença entre `<kbd>`, `<code>` e `<samp>`

Essas três tags são “parentes”, mas cada uma tem seu papel:

|Tag|Significado|Mostra o quê|Exemplo|
|---|---|---|---|
|`<kbd>`|Keyboard|O que o usuário **digita**|`<kbd>Ctrl</kbd> + <kbd>S</kbd>`|
|`<code>`|Code|O **código-fonte** do programa|`<code>console.log("Oi")</code>`|
|`<samp>`|Sample|A **saída** que o computador mostra|`<samp>Oi</samp>`|

💬 Resumindo:

> `<kbd>` → entrada  
> `<code>` → código  
> `<samp>` → saída

---

## 🎨 4️⃣ — Estilo padrão e personalização

Por padrão, o texto dentro de `<kbd>` aparece com **fonte monoespaçada** (tipo máquina de escrever).  
Mas você pode deixar ele com cara de **tecla de teclado real** usando CSS! 💅

### Exemplo:

```html
<p>Pressione <kbd>Ctrl</kbd> + <kbd>S</kbd> para salvar.</p>
```

```css
kbd {
  background-color: #333;
  color: #fff;
  padding: 4px 8px;
  border-radius: 4px;
  border: 1px solid #666;
  font-family: monospace;
  box-shadow: 0 2px 0 #000;
}
```

🪄 Resultado visual:

```
Ctrl  +  S
```

(com cara de teclas do teclado)

---

## 🧩 5️⃣ — Exemplos práticos

### 💻 Atalhos de teclado:

```html
<p>Use <kbd>Ctrl</kbd> + <kbd>C</kbd> para copiar e <kbd>Ctrl</kbd> + <kbd>V</kbd> para colar.</p>
```

### 🧠 Comandos no terminal:

```html
<p>Digite <kbd>git status</kbd> no terminal para verificar o estado do repositório.</p>
```

### 🔐 Senhas ou entradas de texto:

```html
<p>No campo de login, digite <kbd>admin</kbd> como usuário.</p>
```

---

## 🔍 6️⃣ — Acessibilidade e semântica

O `<kbd>` não muda apenas o visual — ele **ajuda o navegador e leitores de tela** a entender que:

> “Isto é uma entrada do usuário (algo digitado).”

Isso torna a página **mais acessível e semântica**.

---

## ✅ 7️⃣ — Resumo rápido:

|Propriedade|Descrição|
|---|---|
|**Nome da tag:**|`<kbd>`|
|**Significa:**|Entrada de teclado (Keyboard input)|
|**Função:**|Mostrar o que o usuário deve digitar|
|**Exemplo:**|`<kbd>Ctrl</kbd> + <kbd>S</kbd>`|
|**Visual padrão:**|Fonte monoespaçada|
|**Usada junto com:**|`<code>` e `<samp>`|

---
