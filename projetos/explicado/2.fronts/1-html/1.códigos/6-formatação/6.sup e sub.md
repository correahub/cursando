
# 🧙‍♂️ Imagine que os números e letras têm superpoderes…

Alguns gostam de **ficar lá em cima**, pulando bem alto.  
Outros gostam de **ficar lá embaixo**, quase como se estivessem escondidos.

No HTML, existem duas tags que fazem isso:

# 🌟 `<sup>` → o que sobe

# 🌟 `<sub>` → o que desce

Vamos entender cada uma delas!

---

# 🦸‍♂️ `<sup>` — O que sobe lá em cima!

**sup = superscript (superescrito)**

Pensa numa letra ou número com **superpoder**, que consegue **subir e ficar lá em cima do texto**.

Exemplo perfeito disso é na matemática:

- 10²
    
- x³
    
- 2⁵
    

O número **2**, **3** e **5** ficam **pequenininhos e lá em cima**, certo?

No HTML isso é feito assim:

```html
10<sup>2</sup>
```

Isso aparece como:

**10²**

---

# 🐾 `<sub>` — O que fica lá embaixo!

**sub = subscript (subescrito)**

Agora pense em uma letra ou número que gosta de ficar **lá embaixo**, quase se escondendo.

Isso é muito usado em:

- fórmulas químicas (H₂O)
    
- matemática
    
- notas de rodapé
    
- certas abreviações
    

Exemplo:

```html
H<sub>2</sub>O
```

Aparece assim:

**H₂O**

O “2” fica pequenininho e lá embaixo.

---

# 🧩 Resumo simples para uma criança

✨ `<sup>` coloca algo **lá em cima**  
✨ `<sub>` coloca algo **lá embaixo**

Como se fosse:

- `<sup>` → o número _pula_
    
- `<sub>` → o número _agacha_
    

---

# 📘 Exemplos do dia a dia

### ⭐ Matemática:

```html
x<sup>3</sup> + 2
```

→ x³ + 2

---

### ⭐ Ciências:

```html
CO<sub>2</sub>
```

→ CO₂

---

### ⭐ Notas de rodapé:

```html
Nota<sup>1</sup>
```

→ Nota¹

---

# 🛑 Importante lembrar

❌ `<sup>` e `<sub>` não são para deixar bonito  
❌ não são para enfeitar palavras aleatórias

✔ Eles são para representar **significados importantes**, como fórmulas e números especiais.

---

# 🧙 Resumo mágico final

|Tag|O que faz?|Como lembrar?|
|---|---|---|
|`<sup>`|Sobe|“Sup” de _super_, fica **lá em cima**|
|`<sub>`|Desce|“Sub” de _submarino_, **vai pra baixo**|
