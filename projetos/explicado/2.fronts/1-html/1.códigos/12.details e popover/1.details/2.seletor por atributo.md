Perfeito! Você pode fazer isso usando o **seletor de atributo** no CSS para detectar quando o `<details>` está **aberto** com o atributo `open`.

---

### ✅ CSS que faz isso:

```css
details[open] {
  background-color: red;
  color: yellow;
}
```

---

### 🔍 Explicação:

- `details[open]` seleciona qualquer `<details>` que **tenha o atributo `open`** (ou seja, está expandido).
    
- `background-color: red;` pinta o fundo de vermelho.
    
- `color: yellow;` muda a cor do texto para amarelo.
    

---

### 💡 Exemplo completo:

```html
<style>
  details {
    margin-bottom: 10px;
    padding: 10px;
    border: 1px solid #ccc;
    border-radius: 5px;
    transition: all 0.3s ease;
  }

  details[open] {
    background-color: red;
    color: yellow;
  }

  summary {
    cursor: pointer;
  }
</style>

<details>
  <summary>O que é HTML?</summary>
  <p>HTML é a linguagem de marcação para criar páginas web.</p>
</details>

<details>
  <summary>O que é CSS?</summary>
  <p>CSS serve para estilizar o HTML.</p>
</details>
```

Se quiser adicionar animações ou mais estilos, posso ajudar também!