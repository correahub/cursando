### 🌉 Passo 1: Criar um lugar para ir (o destino)

Primeiro, a gente precisa de um **ponto de chegada**.  
Esse ponto ganha um **nome especial** chamado **id**.

Por exemplo:

```html
<h2 id="sobre">Sobre Mim</h2>
<p>Eu gosto de programar e aprender coisas novas!</p>
```

👉 Aqui, o `id="sobre"` é o **nome** do lugar onde queremos chegar.

---

### 🚶‍♂️ Passo 2: Criar o link mágico (o caminho)

Agora, criamos um **link** que vai levar até esse lugar.

```html
<a href="#sobre">Ir para a parte Sobre Mim</a>
```

🔍 O `href="#sobre"` quer dizer:

> “Quando eu clicar aqui, quero ir para o lugar com o nome (id) ‘sobre’.”

---

### 🎢 Resultado:

Quando você clica no link, a página **rola sozinha** até o título “Sobre Mim”.  
Não precisa recarregar nada — é só uma viagem dentro da mesma página!

---

### 🌟 Dica extra:

Se quiser que a rolagem seja **suave**, basta colocar isso no seu CSS:

```css
html {
  scroll-behavior: smooth;
}
```

Aí, quando clicar no link, a página **desliza devagarzinho** até o destino — bem bonito! 😎

---

Quer que eu te mostre um **exemplo completo** com tudo funcionando junto (HTML + CSS)?