## 📘 O que é a tag `<dl>`?

A tag `<dl>` é usada para criar uma **lista de definições**. Diferente das listas comuns (`<ul>` e `<ol>`), ela serve para apresentar **termos e suas descrições** — como um pequeno dicionário.

---

## 📌 Estrutura da lista de definições

Ela envolve três elementos principais:

|Tag|Significado|Uso|
|---|---|---|
|`<dl>`|Definition List|Contém toda a lista|
|`<dt>`|Definition Term|O termo que será definido|
|`<dd>`|Definition Description|A descrição do termo|

---

## ✅ Exemplo prático

Vamos imaginar que você está criando um site de glossário com três termos de tecnologia:

<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <title>Glossário de Tecnologia</title>
</head>
<body>

  <h1>Glossário de Tecnologia</h1>

  <dl>
    <dt>HTML</dt>
    <dd>Linguagem de marcação usada para estruturar páginas web.</dd>

    <dt>CSS</dt>
    <dd>Folhas de estilo usadas para definir o visual e o layout das páginas.</dd>

    <dt>JavaScript</dt>
    <dd>Linguagem de programação usada para tornar as páginas interativas.</dd>
  </dl>

</body>
</html>


---

### 🔍 O que este código faz?

- O `<h1>` é o título da página.
    
- O `<dl>` começa a lista de definições.
    
- Cada `<dt>` define um **termo** (como “HTML”, “CSS”...).
    
- Cada `<dd>` explica o termo (como “linguagem de marcação...”).
    
- Você pode repetir quantos pares `<dt> + <dd>` quiser.
    

---

## 🎯 Dica importante

Você pode ter **vários `<dd>` para um único `<dt>`** se quiser dar definições múltiplas.

html

CopiarEditar

`<dt>JavaScript</dt> <dd>Linguagem de programação.</dd> <dd>Usada para interatividade em páginas web.</dd>`