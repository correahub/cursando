## 🗂️ O que é `<optgroup>`?

A tag **`<optgroup>`** é usada dentro de um `<select>` para **organizar as opções em grupos**.

Pense assim:

Imagina que você tem uma lista de frutas e legumes para escolher. Se tudo estiver misturado, fica confuso! 🥦🍎

Com o `<optgroup>`, você pode separar assim:

- 🥕 Legumes:
    
    - Cenoura
        
    - Batata
        
- 🍎 Frutas:
    
    - Maçã
        
    - Banana
        

---

## 📦 Exemplo prático:

```html
<form>
  <label for="comida">Escolha uma comida:</label><br>
  <select id="comida" name="comida">
    <optgroup label="Frutas">
      <option value="maca">Maçã</option>
      <option value="banana">Banana</option>
    </optgroup>
    
    <optgroup label="Legumes">
      <option value="cenoura">Cenoura</option>
      <option value="batata">Batata</option>
    </optgroup>
  </select>
</form>
```

---

## 💡 O que aparece na tela:

```
[ Maçã ⯆ ]
   ▸ Frutas
     - Maçã
     - Banana
   ▸ Legumes
     - Cenoura
     - Batata
```

A pessoa pode ver que as opções estão **bem organizadas**!

---

## 🧠 Explicando cada parte:

| Tag                      | O que faz?                          |
| ------------------------ | ----------------------------------- |
| `label="..."`            | Dá um nome para o grupo             |
| `<select id="" name="">` | Cria o menu                         |
| `<option value"">`       | Itens do menu                       |
| `<optgroup label="">`    | Separa os itens do menu por titulos |

---

## 🎮 Mini desafio!

Tente completar isso:

```html
<select>
  <optgroup label="Bebidas">
    <option value="agua">______</option>
    <option value="suco">______</option>
  </optgroup>

  <optgroup label="Doces">
    <option value="bolo">______</option>
    <option value="sorvete">______</option>
  </optgroup>
</select>
```

Preenche os espaços com nomes! Se quiser, mamãe corrige com carinho! 🥰