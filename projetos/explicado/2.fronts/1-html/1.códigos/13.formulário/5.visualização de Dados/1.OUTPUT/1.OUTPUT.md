### 🧠 O que é o `<output>`?

Imagina que você tem uma **continha de matemática** em um site — tipo somar dois números.  
Você digita os números, clica em um botão, e o resultado **aparece na tela**.  
💡 O lugar onde o resultado aparece é o **output**.

Em HTML, usamos a tag `<output>` para **mostrar o resultado de alguma operação**, normalmente feita com **JavaScript**.

---

### 📦 Estrutura básica

```html
<output></output>
```

Ela é uma **caixinha** onde o resultado aparece.  
Mas sozinha, ela não faz nada — precisamos **ligar ela** a alguma coisa que vai colocar um valor dentro (como um cálculo).

---

### 🧮 Exemplo fácil de entender

Aqui vai um exemplo onde somamos dois números, e o resultado aparece dentro do `<output>`:

```html
<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <title>Exemplo de Output</title>
</head>
<body>
  <h2>Somando números 🧮</h2>

  <label>Primeiro número: 
    <input type="number" id="num1" value="2">
  </label>

  <br><br>

  <label>Segundo número: 
    <input type="number" id="num2" value="3">
  </label>

  <br><br>

  <!-- Botão que faz a conta -->
  <button onclick="somar()">Somar</button>

  <p>O resultado é: <output id="resultado"></output></p>

  <script>
    function somar() {
      // Pega os números digitados
      const n1 = Number(document.getElementById('num1').value);
      const n2 = Number(document.getElementById('num2').value);

      // Mostra o resultado dentro do output
      document.getElementById('resultado').value = n1 + n2;
    }
  </script>
</body>
</html>
```

---

### 🧩 O que está acontecendo aqui:

1. Você digita dois números nos campos `<input>`.
    
2. Clica no botão **Somar**.
    
3. O JavaScript pega esses números e soma.
    
4. O resultado aparece dentro da tag `<output>` (que é como se fosse o “quadro de resultados”).
    

---

### 🗣️ Explicando de um jeito bem simples:

- `<input>` → é onde **você escreve algo**.
    
- `<button>` → é o **botão que faz a mágica acontecer**.
    
- `<output>` → é **onde a resposta aparece**.
    