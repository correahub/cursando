## 📝 O que é `<textarea>`?

A tag **`<textarea>`** cria uma **caixa de texto grandona**, onde a pessoa pode **escrever várias linhas**.  
É diferente do `<input type="text">`, que normalmente só deixa escrever **uma linha**.

---

## 📦 Exemplo simples:

```html
<form>
  <label for="mensagem">Escreva sua mensagem:</label><br>
  <textarea id="mensagem" name="mensagem"></textarea>
</form>
```

---

## 💬 O que aparece na tela?

```
[                                   ]
[                                   ]
[                                   ]
```

Uma caixona onde a pessoa pode digitar textos grandes, como:

- Comentários
    
- Mensagens
    
- Descrições
    
- Histórias!
    

---

## 🧠 Explicando tudo:

|Parte|O que faz?|
|---|---|
|`<textarea>`|Cria a caixona de texto|
|`name="..."`|Dá um nome pra usar no formulário|
|`id="..."`|Liga com o `<label>`|
|Entre a tag de abertura e fechamento você pode colocar um **texto que já aparece preenchido**||

---

## ✨ Com texto dentro:

```html
<textarea name="mensagem">Olá! Eu gostei muito do site.</textarea>
```

Esse texto já aparece dentro da caixa quando ela abrir!

---

## 🧮 Tamanho da caixa:

Você pode usar:

- `rows="4"` → número de **linhas**
    
- `cols="40"` → número de **colunas** (largura)
    

```html
<textarea name="mensagem" rows="4" cols="40"></textarea>
```

Mas hoje em dia a gente geralmente usa CSS pra controlar o tamanho. Isso é só pra entender mesmo. 😄

---

## 🧁 Exemplo divertido: Caixa de recado mágico ✨

```html
<form>
  <label for="recado">Deixe um recado para o Mago:</label><br>
  <textarea id="recado" name="recado" rows="5" cols="30">Olá, Grande Mago...</textarea>
</form>
```

A pessoa pode escrever algo legal para o mago ler! 🧙‍♂️💌

---

## 🎮 Mini desafio:

Complete esse código abaixo:

```html
<form>
  <label for="historia">Conte sua história:</label><br>
  <textarea id="historia" name="historia" rows="6" cols="40">Era uma vez...</textarea>
</form>
```

Você pode mudar o texto dentro do `<textarea>` para inventar uma historinha! ✍️✨

Se quiser, a mamãe pode ler sua história depois, tá bom? 😄💖