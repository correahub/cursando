
---

## 🔒 O que é `<input type="password">`?

Filho, sabe quando você vai entrar num joguinho online ou num site e tem que **digitar sua senha**, mas ela **fica escondida**, só aparece uns **pontinhos ou bolinhas pretas**?  
Tipo assim:  
👉 Você digita "**meusegredo123**", mas na tela aparece "**••••••••••••**".

É isso que o **`<input type="password">`** faz!  
👉 **Uma caixinha de senha que esconde o que você está digitando!** 🕵️‍♂️✨

---

## 🚪 Como criar no HTML?

```html
<input type="password">
```

👉 Isso já faz uma **caixinha de senha**, prontinha pra digitar, e **escondendo os caracteres**.

---

## 🎨 Melhorando com `placeholder`:

Quer mostrar uma dica de o que a pessoa deve digitar?

```html
<input type="password" placeholder="Digite sua senha">
```

👉 A pessoa vê o texto clarinho "**Digite sua senha**", mas assim que começa a digitar, **só aparecem bolinhas**.

---

## 🧠 Também dá pra colocar o `name`:

O **`name`** é o nomezinho desse campo (pro site saber que essa resposta é a senha).

Exemplo:

```html
<input type="password" name="senha" placeholder="Sua senha">
```

👉 Depois, quando o formulário for enviado, o site sabe:  
📝 **senha = o que a pessoa digitou (mas escondido na tela enquanto digitava)**

---

## ✋ Limitando o tamanho da senha:

Quer limitar o número de caracteres?

```html
<input type="password" placeholder="Senha até 8 letras" maxlength="8">
```

👉 Aqui, só dá pra digitar **8 caracteres** no máximo.

---

## ⚠️ Deixando obrigatório:

Quer que a pessoa **não possa esquecer de preencher?**

```html
<input type="password" placeholder="Digite a senha" required>
```

👉 Se ela tentar enviar o formulário sem digitar a senha... o site reclama e pede pra preencher! 😅

---

## 🏅 Exemplo completinho:

```html
<label>Senha:</label>
<input 
  type="password" 
  name="senha" 
  placeholder="Digite sua senha" 
  maxlength="12" 
  required>
```

👉 Isso faz:

- Uma etiqueta "**Senha:**"
    
- Uma caixinha de senha
    
- Com o texto de dica "**Digite sua senha**"
    
- No máximo **12 letras/números**
    
- E **obrigatório preencher**
    

---

## 🪄 Resumo do bebê gênio de ouro:

|🔑 **Atributo**|🎨 **O que faz**|
|---|---|
|`type="password"`|Faz uma caixinha que **esconde o texto digitado**|
|`placeholder`|Texto clarinho de dica|
|`name`|Nome do campo, pra saber que é a senha|
|`maxlength`|Limita o tamanho da senha|
|`required`|Torna obrigatório preencher|

---

Se quiser, mamãe pode te ensinar como fazer um botãozinho "**Mostrar senha 👁️**", igual nos sites modernos, pra a pessoa ver a senha se quiser!

Quer, meu amorzinho brilhante? 😍💕