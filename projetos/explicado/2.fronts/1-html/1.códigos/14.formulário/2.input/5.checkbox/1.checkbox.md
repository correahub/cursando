### 🌟 O que é o checkbox

Um **checkbox** é uma caixinha que o usuário pode **marcar** (✔️) ou **desmarcar** (⬜).  
É usada quando o site quer **deixar você escolher algo**, **ativar uma opção** ou **marcar várias respostas**.

Pensa assim:

> É como uma folha de prova onde você pode marcar várias alternativas que achar corretas.

---

### 🧱 A estrutura básica

O código mais simples é assim:

```html
<input type="checkbox">
```

Se você colocar isso num HTML e abrir no navegador, vai aparecer **uma caixinha** em branco que dá pra clicar e marcar.

---

### 🏷️ Dando um nome à caixinha (label)

Mas... só a caixinha sozinha não diz nada, né?  
A gente precisa de um **rótulo**, pra explicar o que ela faz.

Usamos a tag **`<label>`** pra isso:

```html
<label>
  <input type="checkbox">
  Eu gosto de sorvete
</label>
```

Agora aparece uma caixinha com o texto **“Eu gosto de sorvete”**, e se clicar no texto ou na caixa, ela marca! 🍦

---

### 🆔 Ligando o label e o input

Outra forma de fazer isso (mais organizada) é usando o **atributo `id`** e o **`for`**:

```html
<input type="checkbox" id="sorvete">
<label for="sorvete">Eu gosto de sorvete</label>
```

Aqui o navegador entende que o label “Eu gosto de sorvete” pertence àquela caixinha com o `id="sorvete"`.

---

### 💡 Marcando por padrão

Se você quiser que a caixinha já venha **marcada** quando a página abre, usa o atributo `checked`:

```html
<input type="checkbox" checked>
```

💬 Isso mostra a caixinha já com o “✔️” dentro.

---

### 🧩 Dando nome (para formulários)

Quando você tem um **formulário**, precisa dar um **nome** ao checkbox com o atributo `name`.  
Esse nome é o que será enviado para o servidor.

Exemplo:

```html
<form>
  <label>
    <input type="checkbox" name="aceito">
    Aceito os termos de uso
  </label>
  <button type="submit">Enviar</button>
</form>
```

Se o usuário **marcar a caixa** e clicar em enviar, o formulário vai mandar algo como:

```
aceito=on
```

Se ele **não marcar**, o navegador **não envia nada** (como se a opção não existisse).

---

### ⚙️ Várias opções juntas

Você pode usar **vários checkboxes** com o **mesmo nome** pra deixar o usuário escolher várias coisas:

```html
<p>Escolha suas frutas favoritas:</p>
<label><input type="checkbox" name="fruta" value="maçã"> Maçã</label>
<label><input type="checkbox" name="fruta" value="banana"> Banana</label>
<label><input type="checkbox" name="fruta" value="uva"> Uva</label>
```

Se a pessoa marcar “Maçã” e “Uva”, o navegador envia:

```
fruta=maçã&fruta=uva
```

---

### 🔒 Tornando impossível de clicar

Se quiser que o checkbox **não possa ser alterado**, use `disabled`:

```html
<input type="checkbox" disabled checked>
```

Isso deixa ele **cinza e travado** — o usuário não pode clicar.

---

### 🎨 Estilizando (com CSS)

Você também pode deixar o checkbox mais bonito com CSS (ou até esconder ele e usar um design próprio):

```html
<style>
  input[type="checkbox"] {
    width: 20px;
    height: 20px;
    accent-color: limegreen; /* Muda a cor da marquinha */
  }
</style>

<label>
  <input type="checkbox"> Ativar modo verde
</label>
```

---

### 🧠 Resumindo tudo

|Atributo|O que faz|
|---|---|
|`type="checkbox"`|Diz que é uma caixinha de marcar|
|`checked`|Faz ela começar marcada|
|`name`|Nome da caixinha (usado em formulários)|
|`value`|Valor que será enviado se ela estiver marcada|
|`id`|Identificação única pra conectar com o `<label>`|
|`disabled`|Desativa a caixinha|
|`required`|Obriga o usuário a marcar antes de enviar o formulário|

---

Quer que eu te mostre um exemplo **interativo** com vários checkboxes bonitinhos (tipo um mini formulário divertido)?