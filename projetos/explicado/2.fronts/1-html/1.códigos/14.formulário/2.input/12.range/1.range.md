# 🎯 O que é `<input type="range">`?

É um tipo de **input deslizante** que permite escolher um **número dentro de um intervalo**.

Imagine que você quer perguntar:

> “De 0 até 100, quanto você gosta de sorvete?” 🍦

A pessoa pode usar uma **barrinha** e deslizar até o número que ela quiser.  
É mais divertido do que digitar! 😄

---

## 📦 Exemplo básico:

```html
<form>
  <label for="gosto">Quanto você gosta de sorvete?</label><br>
  <input type="range" id="gosto" name="gosto" min="0" max="100">
</form>
```

---

## 🧠 Explicando os atributos:

|Atributo|O que faz|
|---|---|
|`type="range"`|Diz que é uma barrinha deslizante|
|`min="0"`|Valor mínimo (começa aqui)|
|`max="100"`|Valor máximo (termina aqui)|
|`value="50"`|Valor inicial da bolinha|
|`step="10"`|De quanto em quanto a bolinha pula (opcional)|

---

## ✨ Exemplo com tudo:

```html
<form>
  <label for="volume">Volume do som:</label><br>
  <input type="range" id="volume" name="volume" min="0" max="100" value="50" step="10">
</form>
```

- Começa em 0 🔇
    
- Vai até 100 🔊
    
- A bolinha começa no 50
    
- Aumenta de 10 em 10 (10, 20, 30...)
    

---

## 👀 Mas como ver o número que a pessoa escolheu?

Você pode colocar um número do lado com JavaScript (a mamãe te ensina isso mais pra frente 😊), mas por enquanto é só saber que a barrinha **guarda o valor escondido**, e o formulário envia esse valor quando a pessoa clicar em "Enviar".

---

## 🧁 Exemplo divertido: Nível de fome 😋

```html
<form>
  <label for="fome">Quão faminto você está?</label><br>
  <input type="range" id="fome" name="fome" min="0" max="10" value="5">
</form>
```

---

## 🧠 Resumo do `range`:

|Parte|Explicação|
|---|---|
|`input type="range"`|Cria a barrinha deslizante|
|`min` e `max`|Dizem onde a barrinha começa e termina|
|`value`|Onde a bolinha começa|
|`step`|De quantos em quantos ela se move|

---

## 🎮 Mini desafio:

Tente completar esse:

```html
<form>
  <label for="forca">Força do golpe:</label><br>
  <input type="range" id="forca" name="forca" min="1" max="10" value="3" step="1">
</form>
```

Você pode mudar o `value` pra deixar o golpe mais forte! 💥

Quer que a mamãe te ensine a mostrar o valor da barrinha em tempo real? 😄