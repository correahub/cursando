## 🔍 O que é o `<input type="search">`?

É uma **caixinha de procurar**. Muito parecida com o `<input type="text">`, mas com **algumas melhorias** feitas especialmente para **pesquisa**.

### Exemplo:

```html
<form>
  <label for="busca">Buscar:</label>
  <input type="search" id="busca" name="pesquisa" placeholder="Digite o que quer achar">
  <button type="submit">🔎</button>
</form>
```

---

## 🧒 Explicando passo a passo

### 1. `type="search"`

Diz que esse input é para **pesquisa**.  
Parece um input de texto normal, mas **o navegador entende que você está querendo procurar algo**.

---

### 2. Pode ter um botão de "X" (limpar)

- Em alguns navegadores (como o Chrome), quando você digita algo no `input type="search"`, aparece um **botãozinho "X"** no canto da caixinha.
    
- Esse "X" serve para **apagar tudo rapidinho**! 🧼
    

➡️ Isso **não acontece no `type="text"`** normalmente.

---

### 3. Funciona bem com teclado e voz

- No celular, o teclado pode vir com um botão especial de **"Buscar"** em vez de "Enter".
    
- Pode funcionar melhor com comandos de voz, dependendo do navegador.
    

---

## 🧠 Resumo da diferença entre `text` e `search`

|Tipo|Parecido com...|Tem botão "X"?|Indica que é busca?|Teclado especial (mobile)?|
|---|---|---|---|---|
|`text`|Qualquer texto|❌ Não|❌ Não|Não|
|`search`|Pesquisa|✅ Sim|✅ Sim|Sim (botão de busca)|

---

## ⚙️ Pode usar com outros atributos também!

Você pode adicionar:

- `placeholder="Procure aqui"` → texto de dica
    
- `required` → obriga preencher
    
- `maxlength="50"` → limite de letras
    
- `autocomplete="on"` → guarda buscas anteriores
    

---

### Exemplo completo:

```html
<form action="/procurar">
  <label for="procurar">Procurar algo:</label>
  <input
    type="search"
    id="procurar"
    name="busca"
    placeholder="Digite o que quiser"
    required
    maxlength="30"
    autocomplete="on"
  >
  <button type="submit">🔍 Buscar</button>
</form>
```

---

Se quiser, posso mostrar como isso se comporta em diferentes navegadores ou montar um exemplo que envia dados para ver como o `search` funciona na prática. Quer?