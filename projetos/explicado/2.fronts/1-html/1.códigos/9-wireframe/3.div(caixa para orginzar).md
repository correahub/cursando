### 🧠 Só para você entender (sem precisar usar agora):

A `<div>` é uma **caixa genérica** que não tem significado próprio — ela serve só para **agrupar conteúdo** e aplicar estilos com **CSS** ou scripts com **JavaScript**. Quando você não tem uma tag semântica adequada (como `<section>`, `<header>`, `<footer>`, `<article>`, etc.), aí sim você usa uma `<div>`.