### 🧒 O que é o `input hidden`?

Em HTML, quando usamos formulários, colocamos vários **inputs** — como caixinhas de texto, botões, caixas de seleção — para a pessoa preencher.  
Mas o `input hidden` (em português: **entrada escondida**) é uma caixinha **que o usuário não vê**. Ela **fica invisível na página**, mas **guarda uma informação secreta** para o site usar.

---

### 🧊 Exemplo simples

```html
<form>
  <input type="hidden" name="nivel" value="iniciante">
  <button type="submit">Enviar</button>
</form>
```

#### O que está acontecendo aqui?

1. Temos um formulário.
    
2. Dentro dele, colocamos um **input escondido**:
    
    - `type="hidden"` faz com que ele **não apareça na tela**.
        
    - `name="nivel"` é o **nome da informação**.
        
    - `value="iniciante"` é o **valor escondido**, tipo um bilhete dizendo "ele é iniciante".
        
3. Quando a pessoa clica em **"Enviar"**, o site **envia essa informação escondida** junto com o formulário.
    

---

### 🎒 Por que usar `input hidden`?

Pensa assim:

- Você quer saber quem está enviando o formulário (como um nome de usuário que já está logado).
    
- Ou quer lembrar que o aluno está na **fase 1 do curso**.
    
- Ou quer guardar uma **pontuação secreta**.
    

Mas você **não quer que a pessoa veja ou mude isso na tela**. Então você **esconde** a informação com `input type="hidden"`.

---

### 🕵️ Como uma caixa invisível

É como se fosse:

- Uma **caixinha invisível** que guarda um valor.
    
- Ela não aparece para ninguém.
    
- Mas o site pode **ler essa informação** quando o botão de "Enviar" for clicado.
    

---

### ⚠️ Resumo para não esquecer

|Parte|Significado|
|---|---|
|`type="hidden"`|Diz que é escondido|
|`name="algo"`|Nome da informação|
|`value="valor"`|O que será enviado|
