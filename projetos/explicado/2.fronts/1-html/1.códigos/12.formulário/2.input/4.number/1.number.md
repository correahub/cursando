
---

## 🔢 O que é `<input type="number">`?

Filho, imagina que você quer perguntar pra pessoa:  
— "_Quantos anos você tem?_"  
ou  
— "_Quantos chocolates você quer?_" 🍫

👉 Nesse caso, não faz sentido deixar a pessoa digitar **letras**, né? Só queremos **números**!

💡 É isso que o **`<input type="number">`** faz:  
➡️ Cria uma **caixinha só pra números!**

✔️ Se a pessoa tentar digitar **letras, não deixa!**

---

## 🎯 Como criar no HTML?

```html
<input type="number">
```

👉 Isso já faz uma **caixinha de número**, e aparecem aquelas **setinhas** 🔼🔽 do lado pra aumentar ou diminuir.

---

## 🔥 Melhorando com `placeholder` (dica dentro da caixa):

```html
<input type="number" placeholder="Digite sua idade">
```

👉 Aparece a dica "**Digite sua idade**" dentro da caixinha.

---

## 🚦 Definindo limites com `min` e `max`:

👉 Você pode escolher qual é o **menor** e o **maior número** que a pessoa pode colocar.

### Exemplo:

```html
<input type="number" min="1" max="10" placeholder="De 1 a 10">
```

✔️ Aqui, a pessoa só pode escolher números **de 1 até 10**.  
Se tentar menos ou mais, o site fala: "_Número fora do permitido!_" 🚫

---

## 🪜 Pulando de quantos em quantos (step):

Se quiser que pule de 2 em 2, ou de 5 em 5...

```html
<input type="number" min="0" max="100" step="10" placeholder="De 0 a 100, pulando de 10">
```

👉 A pessoa pode escolher **0, 10, 20, 30... até 100**.

---

## 🧠 Nomeando o campo (`name`):

Isso serve pro site saber **"essa resposta é de quê?"**.

Exemplo:

```html
<input type="number" name="idade" placeholder="Digite sua idade">
```

✔️ Quando a pessoa digita **20**, o site entende:  
📝 **idade = 20**

---

## ⚠️ Deixando obrigatório (`required`):

```html
<input type="number" name="idade" placeholder="Digite sua idade" required>
```

✔️ O site não deixa enviar se você não preencher.

---

## 🏆 Exemplo completinho:

```html
<label>Quantos docinhos você quer? 🍬</label>
<input 
  type="number" 
  name="doces" 
  min="1" 
  max="20" 
  step="1" 
  placeholder="De 1 a 20" 
  required>
```

👉 A pessoa escolhe **quantos docinhos ela quer**, de **1 até 20**, pulando de **1 em 1**, e **não pode deixar em branco**.

---

## 🪄 Resumo do bebê gênio campeão:

|🔑 **Atributo**|🎨 **O que faz**|
|---|---|
|`type="number"`|Cria uma caixinha **só pra números**|
|`placeholder`|Texto clarinho de dica|
|`name`|Dá nome ao campo (ex.: idade, quantidade)|
|`min`|Valor mínimo permitido|
|`max`|Valor máximo permitido|
|`step`|De quanto em quanto aumenta/diminui|
|`required`|Torna obrigatório preencher|

---

Se quiser, mamãe te ensina a fazer uma **calculadora**, ou até um joguinho de **“Adivinhe o número”**, usando esse `input number`! 🎲🧠✨

**Quer, meu amorzinho inteligente?** 🥰💖