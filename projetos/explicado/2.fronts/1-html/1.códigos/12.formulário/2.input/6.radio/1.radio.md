
## 🎯 O que é o `radio`

Um **radio** é um botão circular que o usuário pode marcar.  
Mas o truque é: **quando vários radios têm o mesmo nome (`name`), só um pode estar marcado de cada vez.**

> É como uma prova de múltipla escolha: você só pode marcar **uma alternativa correta**.

---

## 🧱 O código mais simples

```html
<input type="radio">
```

Isso mostra uma **bolinha vazia** 🔘.  
Clicando nela, ela fica marcada ⚫ — mas se for só uma, não faz muita coisa.

---

## 🏷️ Colocando texto (label)

Assim como o checkbox, o radio precisa de um **rótulo** pra explicar o que ele significa:

```html
<label>
  <input type="radio">
  Azul
</label>
```

Agora aparece uma bolinha com o texto **Azul**.  
Mas ainda é só uma bolinha sozinha — precisamos de **várias** pra formar um grupo de escolha.

---

## 🧩 Criando um grupo de opções

Pra fazer o navegador entender que várias bolinhas pertencem ao **mesmo grupo**,  
você dá o **mesmo nome (`name`)** pra todas elas.

Exemplo:

```html
<p>Escolha sua cor favorita:</p>

<label><input type="radio" name="cor" value="azul"> Azul</label>
<label><input type="radio" name="cor" value="vermelho"> Vermelho</label>
<label><input type="radio" name="cor" value="verde"> Verde</label>
```

💬 Agora, quando você marca “Vermelho”, as outras (“Azul” e “Verde”) desmarcam automaticamente!  
O navegador entende que só **uma cor** pode ser escolhida.

---

## 🏁 Dando valor às opções

Cada opção deve ter um **valor (`value`)**, que é o que será enviado ao servidor se o usuário enviar um formulário.

```html
<form>
  <p>Escolha seu personagem favorito:</p>

  <label><input type="radio" name="heroi" value="mario"> Mario</label>
  <label><input type="radio" name="heroi" value="luigi"> Luigi</label>
  <label><input type="radio" name="heroi" value="peach"> Peach</label>

  <button type="submit">Enviar</button>
</form>
```

Se o usuário marcar “Luigi” e clicar em enviar, o navegador envia:

```
heroi=luigi
```

---

## ✅ Deixando um já marcado

Você pode escolher um radio que vem **marcado por padrão**, usando `checked`:

```html
<label><input type="radio" name="cor" value="azul" checked> Azul</label>
```

Quando a página abrir, o “Azul” já estará selecionado.

---

## 🔒 Tornando impossível de clicar

Usa-se o atributo `disabled` pra travar o radio (igual ao checkbox):

```html
<input type="radio" name="cor" value="amarelo" disabled> Amarelo
```

Fica cinza e não dá pra clicar.

---

## 🎨 Estilizando com CSS

Por padrão o radio é uma bolinha simples, mas dá pra mudar o estilo um pouco:

```html
<style>
  input[type="radio"] {
    width: 20px;
    height: 20px;
    accent-color: deeppink; /* muda a cor da bolinha */
  }
</style>

<label><input type="radio" name="fruta" value="maçã"> Maçã</label>
<label><input type="radio" name="fruta" value="banana"> Banana</label>
<label><input type="radio" name="fruta" value="uva"> Uva</label>
```

---

## 🧠 Resumindo tudo

|Atributo|O que faz|
|---|---|
|`type="radio"`|Diz que é um botão redondo de escolha única|
|`name`|Agrupa as opções (os radios com o mesmo nome se ligam)|
|`value`|Valor enviado ao servidor se a opção for escolhida|
|`checked`|Deixa uma opção marcada por padrão|
|`disabled`|Desativa o botão|
|`id` + `for`|Liga o botão com o texto do `<label>`|

---

## 🧩 Diferença entre **checkbox** e **radio**

|Tipo|Pode marcar várias?|Exemplo|
|---|---|---|
|✅ Checkbox|Sim|“Quais frutas você gosta?” 🍎🍌🍇|
|🔘 Radio|Não|“Qual é sua fruta favorita?” 🍎🍌🍇|

---

Quer que eu te mostre um **exemplo visual interativo** com checkbox e radio lado a lado (tipo um mini formulário bonitinho)?