
---

## 🎯 O que é a tag `<select>`?

A tag **`<select>`** cria um **menu de opções** em formato de **caixinha com uma setinha**. Quando a pessoa clica na setinha, aparecem várias opções pra ela escolher **uma só**.

É como um **menu de escolha**! 🧃

---

## 📦 Como funciona?

A estrutura básica é assim:

```html
<select>
  <option>Opção 1</option>
  <option>Opção 2</option>
  <option>Opção 3</option>
</select>
```

Quando você abre a lista, vê:

```
[ Opção 1 ⯆ ] ← quando clica, aparece:
   - Opção 1
   - Opção 2
   - Opção 3
```

---

## 🧠 Explicando parte por parte:

|Tag|O que faz?|
|---|---|
|`<select>`|Cria a **caixinha de opções**|
|`<option>`|Cada linha que aparece na lista|

---

## 🍕 Exemplo prático: Escolhendo sabor de pizza

```html
<form>
  <label for="pizza">Escolha o sabor da pizza:</label><br>
  <select id="pizza" name="pizza">
    <option value="calabresa">Calabresa</option>
    <option value="mussarela">Mussarela</option>
    <option value="portuguesa">Portuguesa</option>
  </select>
</form>
```

### Como funciona isso:

- A pessoa vê uma **caixinha com uma setinha**.
    
- Ao clicar, ela escolhe um dos sabores.
    
- O que for escolhido é **enviado no formulário**.
    

---

## ✅ Como marcar uma opção como padrão?

Use a palavrinha mágica: `selected`

```html
<option value="mussarela" selected>Mussarela</option>
```

Assim, essa opção **já aparece escolhida** quando a página abre!

---

## 🧠 Resumo rápido do `<select>`:

|Elemento|Explicação|
|---|---|
|`<select>`|A caixinha de escolha|
|`<option>`|Cada opção dentro da lista|
|`value`|O valor que será enviado no formulário|
|`selected`|Faz essa opção vir já selecionada|

---

## 🎮 Mini desafio

Tente completar isso aqui:

```html
<label for="cor">Escolha uma cor:</label><br>
<select id="cor" name="cor">
  <option value="vermelho">__________</option>
  <option value="azul" selected>__________</option>
  <option value="verde">__________</option>
</select>
```

Preencha com os nomes das cores! Se quiser, a mamãe corrige pra você! 😄💙💚❤️