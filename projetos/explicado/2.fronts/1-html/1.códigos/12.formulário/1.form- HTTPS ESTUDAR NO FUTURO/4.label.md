## 🏷️ O que é a `<label>`?

A tag **`<label>`** é como uma **plaquinha** que diz o que o usuário deve escrever ou escolher.

### Exemplo real:

Quando você vê uma caixinha assim:

```
[      ] ← Aqui você digita algo...
```

Você precisa saber: **“O que eu coloco aqui?”**

Aí aparece a plaquinha:

```
Nome:
[      ]
```

Essa “plaquinha” é o que a tag `<label>` faz!

---

## 💡 Exemplo prático:

```html
<label>Qual é o seu nome?</label>
<input type="text" name="nome">
```

### O que isso faz?

- A tag `<label>` escreve: **"Qual é o seu nome?"**
    
- A caixinha aparece logo depois para você digitar.
    

---

## 📎 Ligando o `<label>` com o `<input>`

Se quiser deixar tudo **mais certinho e acessível**, podemos usar o **`for`** no label e o **`id`** no input. Isso faz a plaquinha e a caixinha estarem oficialmente conectadas.

### Exemplo:

```html
<label for="nome">Qual é o seu nome?</label>
<input type="text" id="nome" name="nome">
```

> Isso ajuda pessoas que usam leitores de tela a saberem que o label está **ligado** com aquela caixinha.

---

## ✨ O que acontece quando eles estão ligados?

Quando você clica **na palavra do label**, a caixinha automaticamente recebe o foco (ela pisca, pronta pra digitar).

### Exemplo:

👆 Você clica em “Nome:”  
➡️ A caixinha se acende e fica pronta pra escrever!

---

## 🧠 Resumo da tag `<label>`:

|Parte|Explicação|
|---|---|
|`<label>`|Mostra uma “plaquinha” com texto|
|`for="algo"`|Diz com qual caixinha essa plaquinha está conectada|
|Ligada com `id`|Usa `id="algo"` no input correspondente|

---

## 🎨 Mini desafio

Tente completar esse formulário com a tag `<label>`:

```html
<!-- Complete o label e ligue com o input -->
<label for="__idade__">Sua idade:</label>
<input type="number" id="__idade__" name="idade">
```

Consegue completar os espaços?

Se quiser, posso corrigir pra você! 😄