## 🧭 O que é o `<meter>`?

O `<meter>` é uma **barra que mostra um valor dentro de uma faixa**, como se fosse um **medidor de energia**, **vida no jogo**, ou **nível de bateria**.

Mas diferente do `<progress>`, que é usado para **mostrar progresso**, o `<meter>` é usado para **medir algo**, tipo:

- Quanto combustível tem no tanque ⛽
    
- Quanta força você está usando 💪
    
- O nível de perigo de alguma coisa! ☢️
    

---

## ⚡ Exemplo simples:

```html
<p>Nível de energia:</p>
<meter value="0.7" min="0" max="1"></meter>
```

### O que isso quer dizer?

- `value="0.7"` → Está **70% cheio**
    
- `min="0"` → O mínimo é **0** (vazio)
    
- `max="1"` → O máximo é **1** (cheio)
    

A barra vai mostrar assim:

```
[███████     ]
```

---

## 🧃 Explicando com suco:

Imagine que é um **copinho de suco**:

- Ele pode ter de **0 a 1 litro**
    
- E agora tem **0.7 litro**
    

O `<meter>` vai mostrar isso como uma barrinha **quase cheia**.

---

## 🧠 Outros atributos legais (mais avançado, mas fácil de entender):

```html
<meter value="3" min="0" max="10" low="4" high="8" optimum="6"></meter>
```

### O que cada coisa faz?

|Atributo|Significado simples|
|---|---|
|`value`|O valor atual (por exemplo, 3)|
|`min` / `max`|De onde até onde a barra vai (0 a 10)|
|`low`|Abaixo disso é **ruim** (alerta amarelo/vermelho)|
|`high`|Acima disso é muito alto (alerta de outro tipo)|
|`optimum`|Onde seria o **valor ideal**|

Então o navegador **pode pintar a barra de cores diferentes** dependendo se o valor está baixo, alto ou ideal.

---

## 🧸 Resumo de criança:

|Tag|Serve para quê?|
|---|---|
|`<meter>`|É um **medidor**|
|`value`|Quanto tem agora|
|`min` / `max`|O quanto pode ter no mínimo e no máximo|
|`low` / `high` / `optimum`|Servem para dizer o que é ruim, bom ou ideal|

---

## 🧪 Exemplo divertido:

```html
<p>Vida do monstro:</p>
<meter value="25" min="0" max="100" low="30" optimum="70"></meter>
```

> Vai mostrar uma barra quase vazia, em vermelho ou amarelo, porque o monstro está **fraco**!