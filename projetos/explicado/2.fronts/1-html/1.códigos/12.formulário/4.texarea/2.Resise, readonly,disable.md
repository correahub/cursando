## 🧺 `<textarea>`: a caixinha de escrever texto

É como um **bloco de notas**, onde a pessoa pode escrever frases, histórias, ou qualquer coisa.

```html
<textarea></textarea>
```

Agora vamos aprender como mudar o comportamento dela com:

- `resize`
    
- `readonly`
    
- `disabled`
    

---

## ✋ 1. `resize`: controlar o tamanho que a pessoa pode mudar

Por padrão, o usuário pode **puxar os cantinhos** da caixinha para deixá-la maior ou menor.  
Mas com o CSS `resize`, podemos controlar isso.

### Exemplos:

```html
<textarea style="resize: none;"></textarea>
```

|Valor|O que faz|
|---|---|
|`none`|❌ Não pode redimensionar (fixa)|
|`both`|✅ Pode redimensionar horizontal e vertical (padrão)|
|`vertical`|🔃 Só pode mudar pra cima ou pra baixo|
|`horizontal`|↔️ Só pode mudar pros lados|

É **CSS**, não HTML. Coloca dentro do `style=""`.

---

## 🔒 2. `readonly`: só pode ler, não pode escrever

É como se fosse uma caixinha com **texto pronto**, mas você **não pode mudar nada** dentro. Só dá pra **copiar e olhar**.

### Exemplo:

```html
<textarea readonly>Você só pode ler isso.</textarea>
```

- ✅ Pode selecionar e copiar o texto.
    
- ❌ Não pode digitar nada.
    
- ✅ A caixinha ainda **aparece normal**, só não deixa mudar.
    

---

## 🚫 3. `disabled`: está desligada

É como se a caixinha estivesse **desligada** — ela fica **cinza**, e **ninguém pode clicar, escrever ou copiar**.

### Exemplo:

```html
<textarea disabled>Essa caixa está desligada.</textarea>
```

- ❌ Não pode escrever.
    
- ❌ Não pode selecionar ou copiar.
    
- ❌ Não será enviada pelo formulário.
    
- 👀 A aparência muda (fica apagadinha/cinza).
    

---

## 🧠 Resumo rápido:

|Atributo|Pode escrever?|Pode copiar?|Pode enviar no formulário?|Aparece?|
|---|---|---|---|---|
|`resize`|✅|✅|✅|✅|
|`readonly`|❌|✅|✅|✅|
|`disabled`|❌|❌|❌|✅ (cinza)|

---

Se quiser, posso montar uma página HTML com todos esses exemplos funcionando para você testar. Quer?