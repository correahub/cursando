## 🌟 O que é a tag `<caption>`?

Imagine que você fez uma **tabela** para mostrar alguma coisa — por exemplo, os nomes dos seus amigos e a idade deles.

A **tag `<caption>`** é **o título da tabela**.

É como escrever uma plaquinha em cima da tabela dizendo:  
**“O que essa tabela mostra?”**

---

## 🧒 Explicação como para uma criança

Pensa assim:

- Você tem um desenho.
    
- E você coloca um **título** em cima dele, tipo “Minha Família” ou “Meus Animais Favoritos”.
    

A tag `<caption>` faz exatamente isso, mas com **tabelas no HTML**.

Ela ajuda as pessoas (e também computadores, como leitores de tela para pessoas cegas) a entenderem o que a tabela significa.

---

## 🧩 Regras importantes (explicado de forma simples)

1. A `<caption>` **fica dentro da tabela**.
    
2. Ela tem que ser a **primeira coisa** que aparece dentro da tabela.
    
3. Ela **não é uma linha** da tabela: é o **título**.
    

---

## 📘 Exemplo bem fácil

Vamos fazer uma tabela que mostra os **nomes e as idades** de duas pessoas.

```html
<table>
  <caption>Idade das Pessoas</caption>
  <tr>
    <th>Nome</th>
    <th>Idade</th>
  </tr>
  <tr>
    <td>Ana</td>
    <td>10</td>
  </tr>
  <tr>
    <td>Pedro</td>
    <td>12</td>
  </tr>
</table>
```

---

## 🧐 O que está acontecendo aqui?

- `<table>` → começa a tabela
    
- `<caption>` → coloca o **título**: “Idade das Pessoas”
    
- `<tr>` → cria uma **linha**
    
- `<th>` → cabeçalho (como o nome das colunas)
    
- `<td>` → dados da tabela (as informações)
    

---

## 🎯 Resumo bem curtinho

A tag **`<caption>`**:

- É o **título da tabela**
    
- Fica no **topo**, logo depois de `<table>`
    
- Diz às pessoas o **que a tabela mostra**
    

---
