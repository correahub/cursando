### 📝 O que é a tag `<ul>`?

A tag `<ul>` significa **"unordered list"** (lista não ordenada). Ela é usada para criar **listas com marcadores (bolinhas)**. Não importa a ordem dos itens, ela é apenas uma lista de elementos relacionados.

---

### 🔢 O que é a tag `<ol>`?

A tag `<ol>` significa **"ordered list"** (lista ordenada). Ela é usada para criar **listas numeradas**, onde a **ordem dos itens importa** (como passos de uma receita ou instruções).

---

### 🧱 Elemento comum: `<li>`

Tanto `<ul>` quanto `<ol>` usam a tag `<li>` para definir **cada item da lista**.  
**`li` significa "list item"**, ou seja, **item da lista**.

---

### 📌 Exemplo prático completo:

<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <title>Exemplo de Listas</title>
</head>
<body>

  <h2>Lista de compras (sem ordem):</h2>
  <ul>
    <li>Leite</li>
    <li>Pão</li>
    <li>Queijo</li>
    <li>Ovos</li>
  </ul>

  <h2>Passos para fazer um sanduíche (com ordem):</h2>
  <ol>
    <li>Pegue duas fatias de pão</li>
    <li>Coloque queijo e presunto</li>
    <li>Adicione alface e tomate</li>
    <li>Feche o sanduíche e sirva</li>
  </ol>

</body>
</html>
---

### 🧠 Explicação:

- A **primeira lista** usa `<ul>` porque os itens podem estar em qualquer ordem.
    
- A **segunda lista** usa `<ol>` porque segue uma sequência lógica (etapas).
    
- Cada item da lista está dentro de uma tag `<li>`.
    

---

### 🔚 Resumo rápido:

| Tag    | Significado    | Tipo de lista      | Exemplo visual                   |
| ------ | -------------- | ------------------ | -------------------------------- |
| `<ul>` | Unordered list | Lista com bolinhas | • Item 1, • Item 2               |
| `<ol>` | Ordered list   | Lista com números  | 1. Item 1, 2. Item 2             |
| `<li>` | List item      | Item da lista      | Usado dentro de `<ul>` ou `<ol>` |