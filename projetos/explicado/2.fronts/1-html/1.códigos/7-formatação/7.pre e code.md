# 🎮 Imagine que o computador gosta de deixar tudo arrumadinho…

Quando você escreve um texto normal, ele costuma:

- juntar os espaços,
    
- quebrar linhas sozinho,
    
- deixar tudo bonitinho.
    

Mas… e quando você quer mostrar **um código**, com **espaços certinhos**, **linhas certinhas**, tudo exatamente como você escreveu?

Aí entram as tags **`<pre>`** e **`<code>`**! 😄

---

# 🧱 A tag `<pre>` — “Não mexe em nada do que eu escrevi!”

Pensa no `<pre>` como um **cofre mágico** onde tudo que você coloca dentro **fica exatamente igual**:

- os espaços continuam
    
- as quebras de linha continuam
    
- nada muda!
    

É como se você dissesse ao computador:

> “Ei! Não arruma nada! Deixa tudo do jeitinho que eu escrevi!”

🧸 **É o modo 'texto cru'.**

Exemplo:

```html
<pre>
Linha 1
   Linha 2 (com espaços!)
        Linha 3 (com mais espaços!)
</pre>
```

E o navegador vai mostrar **exatamente assim**!  
Com todos os espaços e quebras de linha.

---

# 👾 A tag `<code>` — “Esse texto é um código!”

Agora imagine que você está escrevendo **algo de programação**:

- JavaScript
    
- HTML
    
- Python
    
- etc.
    

O `<code>` serve para **dizer ao navegador**:

> “Ei, isso aqui é um pedacinho de código!”

Ele deixa o texto com cara de código:

- fonte monoespaçada (letra de programador)
    
- tudo bem organizadinho
    
- só muda a aparência, não mexe nos espaços
    

Exemplo:

```html
<code>console.log("Olá!");</code>
```

Fica assim:

```
console.log("Olá!");
```

---

# 🍟 E quando usamos os dois juntos?

Geralmente usamos assim:

- `<code>` = Só mostra que é código
    
- `<pre>` = Mantém tudo exatamente como está
    

🔧 Para mostrar um código grande, com várias linhas, fazemos:

```html
<pre><code>
function ola() {
   console.log("Olá, mundo!");
}
</code></pre>
```

Aí o navegador mostra assim:

```
function ola() {
   console.log("Olá, mundo!");
}
```

Com tudo certinho!  
Linhas, espaços… tudo preservado.

---

# 🧸 Explicação simples como se fosse um brinquedo

Imagine que você tem duas caixas mágicas:

### 📦 `<pre>`

É uma caixa que **nunca bagunça o que você colocou dentro**.  
Se você colocar um papel dobrado, ela deixa dobrado.  
Se você colocar com 10 espaços, ela guarda 10 espaços.

### 📦 `<code>`

É uma caixa que **pinta o papel com “cara de código”**, mas não protege os espaços.

Quando você junta os dois:

➡ `<pre>` protege  
➡ `<code>` estiliza

---

# 🧙‍♂️ Resumo mágico final

|Tag|Pra que serve?|Como lembrar?|
|---|---|---|
|`<pre>`|Mantém tudo exatamente como você escreveu (pré-formatado)|“pre” = **pré-formatado**|
|`<code>`|Diz que o conteúdo é um código|“code” = **código**|
