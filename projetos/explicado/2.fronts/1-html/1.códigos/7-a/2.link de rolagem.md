**# `<a>` com atributo `href` apontando para um # ID é usado para criar um link de rolagem dentro da mesma página em HTML.** Ele permite que o usuário seja levado a um elemento específico identificado por `id`, funcionando como uma âncora. ⚓
