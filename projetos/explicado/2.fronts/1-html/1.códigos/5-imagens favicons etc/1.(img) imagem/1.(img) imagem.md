# 🧸 Imagine que seu site é um álbum de figurinhas…

Quando você quer colocar uma figurinha no seu caderno, você:

👉 pega a figurinha  
👉 cola ela na página

No HTML, para colocar uma **imagem**, você usa a tag:

# 🌟 `<img>`

Ela é como **colar uma figurinha** no seu site!

---

# 🪄 O que a tag `<img>` faz?

A tag `<img>` serve para **mostrar uma imagem** no site.

Ela pode mostrar:

- fotos
    
- desenhos
    
- logos
    
- ícones
    
- memes
    
- qualquer imagem!
    

---

# ⚠️ IMPORTANTE: A tag `<img>` precisa de “ajuda”

Diferente de outras tags, a `<img>` **não funciona sozinha**.  
Ela precisa que você diga **qual imagem** usar.

Como?

Com um atributo chamado **`src`**.

---

# 🧩 O que é `src`?

`src` significa **source** (fonte).  
É o **endereço da imagem**, tipo o caminho até a figurinha.

É como dizer:

👉 “A imagem está aqui, ó!”

---

# 🖼️ Exemplo básico

```html
<img src="gato.png">
```

Isso mostra a imagem chamada **gato.png** no site.

---

# 🧠 Outro atributo importante: `alt`

`alt` significa **alternative text** (texto alternativo).

É um texto que descreve a imagem quando:

- a imagem não carrega
    
- a pessoa usa leitor de tela
    
- ou quando precisamos explicar o que aparece ali
    

É como um rótulo dizendo o que a figurinha mostra.

---

# ✔ Exemplo completo

```html
<img src="gato.png" alt="Um gatinho fofinho sentado.">
```

- `src` → diz **qual** é a imagem
    
- `alt` → diz **o que** tem na imagem
    

---

# 📏 Pode ajustar tamanho também!

A `<img>` permite você definir **largura** (width) e **altura** (height):

```html
<img src="gato.png" alt="Um gato" width="200">
```

Isso faz o gato ficar com **200 pixels de largura**.

---

# 🎨 Você pode usar qualquer tipo de imagem

A tag `<img>` suporta:

✔ .png  
✔ .jpg / .jpeg  
✔ .gif  
✔ .webp  
✔ até .svg

---

# 🎒 Resumo mágico

A tag **`<img>`** é como colar uma figurinha no seu site:

🟦 `src` diz onde está a imagem  
🟦 `alt` descreve a imagem para ajudar leitores e acessibilidade  
🟦 não tem tag de fechamento (não existe `</img>`)  
🟦 você pode controlar o tamanho  
🟦 funciona com vários tipos de arquivos

---

Se quiser, posso te mostrar como colocar imagens da internet, imagens do computador, ou até te ensinar a deixar a imagem redonda com CSS! 😊